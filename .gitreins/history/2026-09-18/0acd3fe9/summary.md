# Verdict: TJ-GAP-066

**Task:** Preserve shell pipeline operators in auto-sandbox reconstruction
**Evaluated:** 2026-09-18T14:21:47.940830
**Result:** ✗ FAIL

## Pipeline Stages

- ✗ **tier1**
  -   ✓ lint: ok (no output)
  ✗ tests: ============================= test session starts ==============================
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
- ✓ **tier2**
  - COMPLETE
  ✓ For every sandboxed pipeline, preserve the original segment count and operator sequence in the modified command, preserve rule provenance, add regression tests, and keep the full suite green.: SEGMENT COUNT + OPERATOR SEQUENCE: parser.py:337 segment_texts(), :346 operator_sequence(), :360 rebuild_command(), :399 structure_preserved(); decider.py:214-250 _rebuild_modified() replaces whole segments in-place inside the ORIGINAL text (operators/whitespace copied verbatim) behind a structure_preserved() guard with a whole-command-wrap fallback. Independent sweep of 160 sandboxed pipeline variants (12 sandbox-matching bases x 10 operator forms, both prefix and suffix positions) via intercept(): operator_sequence(modified)==operator_sequence(orig) AND len(segment_texts(modified))==len(parse_command(orig)) for ALL 160, bad=0. Spot checks: 'go test ./... | tee /tmp/log' -> "unshare --user --pid --fork --kill-child=SIGKILL bash -c 'go test ./...' | tee /tmp/log"; 'make | tee build.log | tail -3' -> "... 'make' | tee build.log | tail -3"; 'go test 2>&1 | tee f' -> "... 'go test' 2>&1 | tee f"; 'ls | go test ./...' -> "ls | unshare ... 'go test ./...'". PRE-FIX at HEAD~1 confirmed broken: same command returned "unshare ... bash -c 'go test ./...' tee /tmp/log" (pipe dropped, tee became an argument). END-TO-END: standalone/terminal-jail:261-274 executes `modified` verbatim via bash -c; `./standalone/terminal-jail bash -c 'echo hello-from-stage1 | tee /tmp/tj066.log'` printed 'hello-from-stage1' AND wrote /tmp/tj066.log, proving tee ran as a real pipeline stage. RULE PROVENANCE: decider.py:150-153,186-192 sets modify_rule_id from the first rewritten segment and passes rule_id= on the aggregate MODIFY; verified intercept('go test ./... | tee /tmp/log').rule_id=='auto-go-test', 'npm test | tail -5'->'auto-npm-test', 'make | ...'->'auto-make', 'python3 run_tests.py 2>&1 | tee f'->'auto-script' (PRE-FIX was None). REGRESSION TESTS: test_interruptor.py +318 lines (TestSandboxPipelineReconstruction 5 parametrized pipeline vectors + trailing-stage-not-swallowed + every-stage-wrapped + allowed-stage-then-sandboxed + single-command-unchanged; TestSandboxReconstructionRefusal 5 tests incl. guard rejection and escaping-hazard fallback; TestCommandRebuild 6 tests incl. segment_texts_mirrors_parse_command corpus); test_interruptor_integration.py +test_bridge_preserves_pipeline_operators_in_modified and a rule_id assertion in test_bridge_sandboxes_quoted_pytest; test_escape_waves.py +rule_id provenance assertion. Ran `python -m pytest -v test_interruptor.py -k "Rebuild or Pipeline or Reconstruction"` -> 22 passed, 0 failed. FULL SUITE GREEN: `cd plugin && python -m pytest -q --tb=short --ignore=test_packaging.py --ignore=test_metrics_export.py` -> exit 0, '557 passed, 5 skipped in 13.21s'; repo-root run -> exit 0, '557 passed, 5 skipped in 15.47s'. The two excluded modules fail for PRE-EXISTING environment reasons unrelated to this diff (test_packaging.py: ModuleNotFoundError: No module named 'tomllib'; test_metrics_export.py 20 failures from scripts/metrics-export.py:16 `from datetime import UTC` ImportError on the Python 3.10 host) — both reproduce identically at HEAD~1 with the same interpreter and neither file is touched by this change. LSP diagnostics: 0 findings. Dead-code scan shows no unused new helpers (all four are imported by decider.py and the tests).


## Summary

Judge Result: TJ-GAP-066

Stage tier1: FAIL
    ✓ lint: ok (no output)
  ✗ tests: ============================= test session starts ==============================
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)

Stage tier2: PASS
  COMPLETE
  ✓ For every sandboxed pipeline, preserve the original segment count and operator sequence in the modified command, preserve rule provenance, add regression tests, and keep the full suite green.: SEGMENT COUNT + OPERATOR SEQUENCE: parser.py:337 segment_texts(), :346 operator_sequence(), :360 rebuild_command(), :399 structure_preserved(); decider.py:214-250 _rebuild_modified() replaces whole segments in-place inside the ORIGINAL text (operators/whitespace copied verbatim) behind a structure_preserved() guard with a whole-command-wrap fallback. Independent sweep of 160 sandboxed pipeline variants (12 sandbox-matching bases x 10 operator forms, both prefix and suffix positions) via intercept(): operator_sequence(modified)==operator_sequence(orig) AND len(segment_texts(modified))==len(parse_command(orig)) for ALL 160, bad=0. Spot checks: 'go test ./... | tee /tmp/log' -> "unshare --user --pid --fork --kill-child=SIGKILL bash -c 'go test ./...' | tee /tmp/log"; 'make | tee build.log | tail -3' -> "... 'make' | tee build.log | tail -3"; 'go test 2>&1 | tee f' -> "... 'go test' 2>&1 | tee f"; 'ls | go test ./...' -> "ls | unshare ... 'go test ./...'". PRE-FIX at HEAD~1 confirmed broken: same command returned "unshare ... bash -c 'go test ./...' tee /tmp/log" (pipe dropped, tee became an argument). END-TO-END: standalone/terminal-jail:261-274 executes `modified` verbatim via bash -c; `./standalone/terminal-jail bash -c 'echo hello-from-stage1 | tee /tmp/tj066.log'` printed 'hello-from-stage1' AND wrote /tmp/tj066.log, proving tee ran as a real pipeline stage. RULE PROVENANCE: decider.py:150-153,186-192 sets modify_rule_id from the first rewritten segment and passes rule_id= on the aggregate MODIFY; verified intercept('go test ./... | tee /tmp/log').rule_id=='auto-go-test', 'npm test | tail -5'->'auto-npm-test', 'make | ...'->'auto-make', 'python3 run_tests.py 2>&1 | tee f'->'auto-script' (PRE-FIX was None). REGRESSION TESTS: test_interruptor.py +318 lines (TestSandboxPipelineReconstruction 5 parametrized pipeline vectors + trailing-stage-not-swallowed + every-stage-wrapped + allowed-stage-then-sandboxed + single-command-unchanged; TestSandboxReconstructionRefusal 5 tests incl. guard rejection and escaping-hazard fallback; TestCommandRebuild 6 tests incl. segment_texts_mirrors_parse_command corpus); test_interruptor_integration.py +test_bridge_preserves_pipeline_operators_in_modified and a rule_id assertion in test_bridge_sandboxes_quoted_pytest; test_escape_waves.py +rule_id provenance assertion. Ran `python -m pytest -v test_interruptor.py -k "Rebuild or Pipeline or Reconstruction"` -> 22 passed, 0 failed. FULL SUITE GREEN: `cd plugin && python -m pytest -q --tb=short --ignore=test_packaging.py --ignore=test_metrics_export.py` -> exit 0, '557 passed, 5 skipped in 13.21s'; repo-root run -> exit 0, '557 passed, 5 skipped in 15.47s'. The two excluded modules fail for PRE-EXISTING environment reasons unrelated to this diff (test_packaging.py: ModuleNotFoundError: No module named 'tomllib'; test_metrics_export.py 20 failures from scripts/metrics-export.py:16 `from datetime import UTC` ImportError on the Python 3.10 host) — both reproduce identically at HEAD~1 with the same interpreter and neither file is touched by this change. LSP diagnostics: 0 findings. Dead-code scan shows no unused new helpers (all four are imported by decider.py and the tests).


Overall: FAIL ✗
