# Verdict: tj-gap-054

**Task:** Optional bubblewrap jail backend
**Evaluated:** 2026-09-18T12:12:34.590450
**Result:** ✗ FAIL

## Pipeline Stages

- ✗ **tier1**
  -   ✓ lint: ok (no output)
  ✗ tests: ============================= test session starts ==============================
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
- ✓ **tier2**
  - COMPLETE
  ✓ Add runtime-detected bubblewrap backend with private /proc and --die-with-parent, preserve unshare fallback, add tests and docs; no regressions in existing wrapper behavior.: IMPLEMENTATION (standalone/terminal-jail, commit 505315a): runtime detection at line 442 `_TJ_BWRAP_BIN="$(command -v bwrap 2>/dev/null || true)"` (never assumed present); selector `TERMINAL_JAIL_JAIL_BACKEND=auto|bwrap|unshare` validated at lines 186-191 (unknown value -> exit 2 before any namespace work); BWRAP_FLAGS = `--unshare-user --unshare-pid --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc` (--proc /proc = private /proc; --die-with-parent = PR_SET_PDEATHSIG; --as-pid-1 deliberately absent). Selection at lines 452-475: bwrap demand fails closed (exit 2, command not run) on missing binary or failed probe, never silently downgraded; auto uses bwrap when present+probe-green else unshare, with a loud WARNING that the fallback 'does NOT provide a private /proc'; unshare pins prior behavior. LIVE EVIDENCE: private /proc confirmed — jail procs=5, init=bwrap, ns=pid:[4026538975] vs host procs=1660, init=systemd, ns=pid:[4026531836]. --die-with-parent confirmed — payload PID 2842438 present before SIGKILL of wrapper, absent after (no orphan). UNSHARE FALLBACK PRESERVED: `git diff HEAD~1 HEAD -- standalone/terminal-jail | grep -E 'exec unshare|UNSHARE_FLAGS='` returned ZERO changed lines (byte-for-byte); pinned `TERMINAL_JAIL_JAIL_BACKEND=unshare` and all `--user` paths still emit the pre-existing degradation warning and rc=0. TESTS: `cd /home/kara/terminal-jail && .venv/bin/python -m pytest -x --tb=short` (the configured guards.test_command) -> EXIT_CODE=0, '561 passed, 5 skipped in 18.51s'; baseline HEAD~1 -> '536 passed, 14 skipped in 11.51s' => +25 tests, 0 failures, no regressions. New plugin/test_backend_selection.py -> 16 passed in 0.51s including 2 LIVE tests (test_live_bwrap_private_proc_and_new_pid_namespace, test_live_bwrap_argv_and_exit_semantics) that ran rather than skipped. DOCS: README.md (lines 13, 24-36, 48, 61, 200-203, 314, 323, 333), specs/cli.md (lines 15-36, 149-156, 183-200, new section 4 'Jail backends' at 217-231 with selection-policy and per-backend property tables, test matrix CLI-17..CLI-25), docs/quickstart.md (100-143, 326-337), docs/deploy-to-karahermes.md, CHANGELOG.md, and `--help` output. LINT/STATIC: ruff 'All checks passed!' rc=0; `bash -n standalone/terminal-jail` syntax OK; LSP diagnostics 0 findings. The plugin/test_install.py edit only pins TERMINAL_JAIL_JAIL_BACKEND=unshare on one test with a docstring explaining why (auto would legitimately hand off to bwrap) — a documented adaptation, not regression masking.
The bubblewrap backend is fully implemented with runtime detection, private /proc and --die-with-parent (both verified live), the unshare fallback is preserved byte-for-byte, 16 new tests plus docs were added, and the full suite passes 561/5-skipped with zero regressions against a 536/14 baseline.

## Summary

Judge Result: tj-gap-054

Stage tier1: FAIL
    ✓ lint: ok (no output)
  ✗ tests: ============================= test session starts ==============================
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)

Stage tier2: PASS
  COMPLETE
  ✓ Add runtime-detected bubblewrap backend with private /proc and --die-with-parent, preserve unshare fallback, add tests and docs; no regressions in existing wrapper behavior.: IMPLEMENTATION (standalone/terminal-jail, commit 505315a): runtime detection at line 442 `_TJ_BWRAP_BIN="$(command -v bwrap 2>/dev/null || true)"` (never assumed present); selector `TERMINAL_JAIL_JAIL_BACKEND=auto|bwrap|unshare` validated at lines 186-191 (unknown value -> exit 2 before any namespace work); BWRAP_FLAGS = `--unshare-user --unshare-pid --die-with-parent --bind / / --dev-bind /dev /dev --proc /proc` (--proc /proc = private /proc; --die-with-parent = PR_SET_PDEATHSIG; --as-pid-1 deliberately absent). Selection at lines 452-475: bwrap demand fails closed (exit 2, command not run) on missing binary or failed probe, never silently downgraded; auto uses bwrap when present+probe-green else unshare, with a loud WARNING that the fallback 'does NOT provide a private /proc'; unshare pins prior behavior. LIVE EVIDENCE: private /proc confirmed — jail procs=5, init=bwrap, ns=pid:[4026538975] vs host procs=1660, init=systemd, ns=pid:[4026531836]. --die-with-parent confirmed — payload PID 2842438 present before SIGKILL of wrapper, absent after (no orphan). UNSHARE FALLBACK PRESERVED: `git diff HEAD~1 HEAD -- standalone/terminal-jail | grep -E 'exec unshare|UNSHARE_FLAGS='` returned ZERO changed lines (byte-for-byte); pinned `TERMINAL_JAIL_JAIL_BACKEND=unshare` and all `--user` paths still emit the pre-existing degradation warning and rc=0. TESTS: `cd /home/kara/terminal-jail && .venv/bin/python -m pytest -x --tb=short` (the configured guards.test_command) -> EXIT_CODE=0, '561 passed, 5 skipped in 18.51s'; baseline HEAD~1 -> '536 passed, 14 skipped in 11.51s' => +25 tests, 0 failures, no regressions. New plugin/test_backend_selection.py -> 16 passed in 0.51s including 2 LIVE tests (test_live_bwrap_private_proc_and_new_pid_namespace, test_live_bwrap_argv_and_exit_semantics) that ran rather than skipped. DOCS: README.md (lines 13, 24-36, 48, 61, 200-203, 314, 323, 333), specs/cli.md (lines 15-36, 149-156, 183-200, new section 4 'Jail backends' at 217-231 with selection-policy and per-backend property tables, test matrix CLI-17..CLI-25), docs/quickstart.md (100-143, 326-337), docs/deploy-to-karahermes.md, CHANGELOG.md, and `--help` output. LINT/STATIC: ruff 'All checks passed!' rc=0; `bash -n standalone/terminal-jail` syntax OK; LSP diagnostics 0 findings. The plugin/test_install.py edit only pins TERMINAL_JAIL_JAIL_BACKEND=unshare on one test with a docstring explaining why (auto would legitimately hand off to bwrap) — a documented adaptation, not regression masking.
The bubblewrap backend is fully implemented with runtime detection, private /proc and --die-with-parent (both verified live), the unshare fallback is preserved byte-for-byte, 16 new tests plus docs were added, and the full suite passes 561/5-skipped with zero regressions against a 536/14 baseline.

Overall: FAIL ✗
