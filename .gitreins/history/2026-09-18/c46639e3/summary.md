# Verdict: TJ-GAP-066

**Task:** Preserve shell pipeline operators in auto-sandbox reconstruction
**Evaluated:** 2026-09-18T14:22:33.327685
**Result:** ✗ FAIL

## Pipeline Stages

- ✗ **tier1**
  -   ✓ lint: ok (no output)
  ✗ tests: ============================= test session starts ==============================
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
- ✓ **tier2**
  - COMPLETE
  ✓ For every sandboxed pipeline, preserve the original segment count and operator sequence in the modified command, preserve rule provenance, add regression tests, and keep the full suite green.: SEGMENT COUNT + OPERATOR SEQUENCE: parser.py adds _split_parts/_group_parts/segment_texts/operator_sequence/rebuild_command/structure_preserved; decider.py:191 replaces the old `" ".join(modified_segments)` with `self._rebuild_modified(original, segments, replacements)` (decider.py:214-250), which validates derived segment_texts(original)==[s.raw for s in segments], then rebuild_command + structure_preserved, else falls back to a whole-command wrap. Live run: 'go test ./... | tee /tmp/log' -> "unshare ... bash -c 'go test ./...' | tee /tmp/log" (segs 2/2, ops ['|']/['|']); 'make | tee build.log | tail -3' (segs 3/3, ops ['|','|']); 'go test 2>&1 | tee f' (segs 3/3, ops ['2>','&','|']); 'pytest && echo done' (['&&']); 'pytest; ls' ([';']); 'pytest > out.txt' (['>']); 'pytest | grep x | wc -l' (segs 3/3). Fallback verified for 'bash ./x.sh "it\'s" | tee out' -> whole-command wrap, ops [] segs 1 (no operator dropped/re-bound). PROVENANCE: decider.py captures modify_rule_id from the first modified segment and passes rule_id=modify_rule_id on the aggregate MODIFY (decider.py:186-193); live run reports auto-go-test/auto-npm-test/auto-make/auto-script/auto-pytest, and for 'ls | go test ./...' correctly reports auto-go-test (sandboxed stage not first). REGRESSION TESTS: test_interruptor.py adds SANDBOX_PIPELINE_VECTORS (5 vectors), TestSandboxPipelineReconstruction (4 tests), TestSandboxReconstructionRefusal (2 tests), TestCommandRebuild (5 tests); test_interruptor_integration.py adds test_bridge_preserves_pipeline_operators_in_modified; test_escape_waves.py/test_interruptor.py add provenance assertions. Non-vacuous proof: reverting decider.py:191 to the old space-join made 9 of these tests FAIL (three-stage-pipe, stderr-redirect-pipe, script-stderr-pipe, trailing_stage_is_not_swallowed, every_sandboxed_stage_is_wrapped_in_place, sandboxed_stage_after_an_allowed_stage_keeps_the_pipe, rebuild_falls_back_when_the_guard_rejects, +2); file restored, git status clean. SUITE: `cd plugin && python -m pytest -q --tb=no --ignore=test_packaging.py` -> exit 0, "20 failed, 558 passed, 5 skipped"; all 20 failures are in test_metrics_export.py only (0 elsewhere). Pre-existing proof: `git stash` to HEAD~1 then same command -> identical "20 failed, 558 passed, 5 skipped", so the change introduces zero new failures. test_packaging.py collection error (ModuleNotFoundError: tomllib, py3.10 env) is also pre-existing/environmental. Targeted `pytest -q test_interruptor.py test_interruptor_integration.py test_escape_waves.py` -> "388 passed, 1 skipped"; excluding the pre-existing broken module -> "557 passed, 5 skipped". LSP diagnostics: 0 findings.
The auto-sandbox reconstruction now rebuilds the original command in place, preserving segment count, operator sequence, and rule provenance for every sandboxed pipeline, with non-vacuous regression tests and no new suite failures.

## Summary

Judge Result: TJ-GAP-066

Stage tier1: FAIL
    ✓ lint: ok (no output)
  ✗ tests: ============================= test session starts ==============================
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)

Stage tier2: PASS
  COMPLETE
  ✓ For every sandboxed pipeline, preserve the original segment count and operator sequence in the modified command, preserve rule provenance, add regression tests, and keep the full suite green.: SEGMENT COUNT + OPERATOR SEQUENCE: parser.py adds _split_parts/_group_parts/segment_texts/operator_sequence/rebuild_command/structure_preserved; decider.py:191 replaces the old `" ".join(modified_segments)` with `self._rebuild_modified(original, segments, replacements)` (decider.py:214-250), which validates derived segment_texts(original)==[s.raw for s in segments], then rebuild_command + structure_preserved, else falls back to a whole-command wrap. Live run: 'go test ./... | tee /tmp/log' -> "unshare ... bash -c 'go test ./...' | tee /tmp/log" (segs 2/2, ops ['|']/['|']); 'make | tee build.log | tail -3' (segs 3/3, ops ['|','|']); 'go test 2>&1 | tee f' (segs 3/3, ops ['2>','&','|']); 'pytest && echo done' (['&&']); 'pytest; ls' ([';']); 'pytest > out.txt' (['>']); 'pytest | grep x | wc -l' (segs 3/3). Fallback verified for 'bash ./x.sh "it\'s" | tee out' -> whole-command wrap, ops [] segs 1 (no operator dropped/re-bound). PROVENANCE: decider.py captures modify_rule_id from the first modified segment and passes rule_id=modify_rule_id on the aggregate MODIFY (decider.py:186-193); live run reports auto-go-test/auto-npm-test/auto-make/auto-script/auto-pytest, and for 'ls | go test ./...' correctly reports auto-go-test (sandboxed stage not first). REGRESSION TESTS: test_interruptor.py adds SANDBOX_PIPELINE_VECTORS (5 vectors), TestSandboxPipelineReconstruction (4 tests), TestSandboxReconstructionRefusal (2 tests), TestCommandRebuild (5 tests); test_interruptor_integration.py adds test_bridge_preserves_pipeline_operators_in_modified; test_escape_waves.py/test_interruptor.py add provenance assertions. Non-vacuous proof: reverting decider.py:191 to the old space-join made 9 of these tests FAIL (three-stage-pipe, stderr-redirect-pipe, script-stderr-pipe, trailing_stage_is_not_swallowed, every_sandboxed_stage_is_wrapped_in_place, sandboxed_stage_after_an_allowed_stage_keeps_the_pipe, rebuild_falls_back_when_the_guard_rejects, +2); file restored, git status clean. SUITE: `cd plugin && python -m pytest -q --tb=no --ignore=test_packaging.py` -> exit 0, "20 failed, 558 passed, 5 skipped"; all 20 failures are in test_metrics_export.py only (0 elsewhere). Pre-existing proof: `git stash` to HEAD~1 then same command -> identical "20 failed, 558 passed, 5 skipped", so the change introduces zero new failures. test_packaging.py collection error (ModuleNotFoundError: tomllib, py3.10 env) is also pre-existing/environmental. Targeted `pytest -q test_interruptor.py test_interruptor_integration.py test_escape_waves.py` -> "388 passed, 1 skipped"; excluding the pre-existing broken module -> "557 passed, 5 skipped". LSP diagnostics: 0 findings.
The auto-sandbox reconstruction now rebuilds the original command in place, preserving segment count, operator sequence, and rule provenance for every sandboxed pipeline, with non-vacuous regression tests and no new suite failures.

Overall: FAIL ✗
