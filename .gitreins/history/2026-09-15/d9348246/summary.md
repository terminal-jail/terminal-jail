# Verdict: TJ-DF-015

**Task:** P0: --user jail has NO filesystem isolation (unmapped userns) — implement real uid mapping with loud degraded fallback
**Evaluated:** 2026-09-15T12:07:12.850818
**Result:** ✓ PASS

## Pipeline Stages

- ✓ **tier1**
  -   ✓ lint: 
  ✓ secrets: [90m7:05AM[0m [32mINF[0m [1mscanned ~3322470 bytes (3.32 MB) in 218ms[0m
[90m7:05AM[0m [32m
  ✓ tests: ============================= test session starts ==============================
platform linux -- P
- ✓ **tier2**
  - COMPLETE
  ✓ The standalone wrapper and the engine auto-sandbox prefix both use a mapping-less unshare --user; after the fix, a mapped launch (--map-users=<nobody>:<subuid>:1 with -S/-G) is attempted first and the wrapper exports TERMINAL_JAIL_FS_ISOLATION=mapped when it succeeds: standalone/terminal-jail:307-345 builds _TJ_MAPPED_FLAGS="--user --map-users=65534:${_tj_subuid}:1 --map-groups=65534:${_tj_subgid}:1 -S 65534 -G 65534 --pid --fork --kill-child=SIGKILL", probes it with `unshare $_TJ_MAPPED_FLAGS true` FIRST, sets TERMINAL_JAIL_FS_ISOLATION=mapped on success and exports it. Engine: plugin/terminal_jail/interruptor/userns.py:153-166 unshare_prefix() returns the mapped prefix when mapped_launch_ok(); decider.py:30 `_UNSHARE_PREFIX = unshare_prefix()`. Live proof of the success branch: `sudo -n ./standalone/terminal-jail --user -- sh -c 'echo FS=$TERMINAL_JAIL_FS_ISOLATION; id'` -> `FS=mapped` / `uid=65534(nobody)`, EXIT=0. Engine mapped-host prefix (probe stubbed True) = `unshare --user --map-users=65534:100000:1 --map-groups=65534:100000:1 -S 65534 -G 65534 --pid --fork --kill-child=SIGKILL bash -c `.
  ✓ Where the host forbids the uid mapping (this host: AppArmor unprivileged_userns profile denies setuid), --user still exits 0 but prints a loud warning that filesystem isolation is NOT active and names the cause; rc semantics for bare mode are unchanged (no silent auto-fallback): `./standalone/terminal-jail --user -- id` on this host prints to stderr: "terminal-jail: WARNING: no filesystem isolation — could not create a uid mapping under the user namespace (no filesystem isolation: this host denies setuid inside unprivileged user namespaces, e.g. Ubuntu AppArmor profile 'unprivileged_userns'; see scripts/fs-isolation-probe.py)" and EXIT=0; inside, TERMINAL_JAIL_FS_ISOLATION=degraded. Host confirms the cause: /proc/sys/kernel/apparmor_restrict_unprivileged_userns=1. Bare mode unchanged: standalone/terminal-jail:365-372 still exits 2 with a message on namespace-creation failure and never auto-adds --user (README.md:250 documents 'no automatic fallback ... never silently downgrades isolation').
  ✓ scripts/fs-isolation-probe.py reports the filesystem-isolation layer honestly (FULL/DEGRADED + cause) and always exits 0; on this host it prints DEGRADED with the uid-mapping cause: `./scripts/fs-isolation-probe.py` -> "DEGRADED: mapped launch failed (rc=1, stderr='unshare: setgroups failed: Operation not permitted'); causes: uid-mapping denied: mapped launch failed while the legacy one succeeded (kernel denies setgid/setgroups inside unprivileged user namespaces — AppArmor profile 'unprivileged_userns') [kernel.apparmor_restrict_unprivileged_userns=1]. remediation: ..." with exit=0. Honesty proven both ways: `sudo -n ./scripts/fs-isolation-probe.py` -> "FULL: uid-mapped user namespace denies caller-owned 600 reads and home writes", exit=0. main() (scripts/fs-isolation-probe.py:194-195) only prints — no nonzero sys.exit path, so it always exits 0.
  ✓ Docs no longer claim uid/filesystem isolation: README, docs/quickstart.md, specs/cli.md and the in-repo skills/terminal-jail-usage/SKILL.md state that --user = PID lifecycle containment + env scrub, and that filesystem isolation requires a working uid mapping: All four files updated in commit f0cc29b. grep for stale claims ("UID isolation", "runs as nobody=65534", "trades /proc isolation for UID") across README.md, docs/quickstart.md, specs/cli.md, skills/terminal-jail-usage/SKILL.md returns ZERO matches. New text present: specs/cli.md:79 and :170 ("--user ... for PID-namespace lifecycle containment + identity env scrub. Filesystem isolation requires a uid mapping (--map-users/--map-groups with -S/-G); it is active ONLY when the exact-flags preflight passes (TERMINAL_JAIL_FS_ISOLATION=mapped)"); README.md:250 ("--user itself has two tiers ... real filesystem isolation, TERMINAL_JAIL_FS_ISOLATION=mapped ... otherwise ... loud `no filesystem isolation` warning"); docs/quickstart.md FAQ "Does --user isolate the filesystem? Only when the host allows a uid mapping"; skills/terminal-jail-usage/SKILL.md item 3 ("--user filesystem isolation is CONDITIONAL (TJ-DF-015, fixed 2026-09-15)").
  ✓ The mapped recipe is proven to actually isolate where the host permits it: the same flags run under sudo yield uid=65534 and EACCES on a caller-owned 600 file; the plugin test suite stays green (no new failures vs 334 passed/13 skipped): Mapped recipe under sudo: `sudo -n unshare --user --map-users=65534:100000:1 --map-groups=65534:100000:1 -S 65534 -G 65534 --pid --fork --kill-child=SIGKILL id` -> `uid=65534(nobody) gid=65534(nogroup)`, EXIT=0; same flags with `cat /tmp/tj015/secret600` (caller-owned, mode 600) -> `cat: /tmp/tj015/secret600: Permission denied`, EXIT=1 (EACCES). Test suite: baseline worktree at a94af73 (/tmp/tjbase) = `334 passed, 13 skipped`; HEAD f0cc29b = `348 passed, 14 skipped in 8.40s` via the configured `pytest -x --tb=short` (exit 0) — delta is exactly the new plugin/test_userns.py (14 passed, 1 skipped), so no new failures. LSP diagnostics: 0 findings.
All five criteria verified with live command evidence: mapped uid launch attempted first with TERMINAL_JAIL_FS_ISOLATION=mapped on success (proven under sudo), loud exit-0 degraded warning naming the AppArmor cause on this host, honest FULL/DEGRADED probe that always exits 0, all four docs corrected with no stale isolation claims, and the mapped recipe proven to yield uid=65534 + EACCES while the suite went 334/13 -> 348/14 with no new failures.

## Summary

Judge Result: TJ-DF-015

Stage tier1: PASS
    ✓ lint: 
  ✓ secrets: [90m7:05AM[0m [32mINF[0m [1mscanned ~3322470 bytes (3.32 MB) in 218ms[0m
[90m7:05AM[0m [32m
  ✓ tests: ============================= test session starts ==============================
platform linux -- P

Stage tier2: PASS
  COMPLETE
  ✓ The standalone wrapper and the engine auto-sandbox prefix both use a mapping-less unshare --user; after the fix, a mapped launch (--map-users=<nobody>:<subuid>:1 with -S/-G) is attempted first and the wrapper exports TERMINAL_JAIL_FS_ISOLATION=mapped when it succeeds: standalone/terminal-jail:307-345 builds _TJ_MAPPED_FLAGS="--user --map-users=65534:${_tj_subuid}:1 --map-groups=65534:${_tj_subgid}:1 -S 65534 -G 65534 --pid --fork --kill-child=SIGKILL", probes it with `unshare $_TJ_MAPPED_FLAGS true` FIRST, sets TERMINAL_JAIL_FS_ISOLATION=mapped on success and exports it. Engine: plugin/terminal_jail/interruptor/userns.py:153-166 unshare_prefix() returns the mapped prefix when mapped_launch_ok(); decider.py:30 `_UNSHARE_PREFIX = unshare_prefix()`. Live proof of the success branch: `sudo -n ./standalone/terminal-jail --user -- sh -c 'echo FS=$TERMINAL_JAIL_FS_ISOLATION; id'` -> `FS=mapped` / `uid=65534(nobody)`, EXIT=0. Engine mapped-host prefix (probe stubbed True) = `unshare --user --map-users=65534:100000:1 --map-groups=65534:100000:1 -S 65534 -G 65534 --pid --fork --kill-child=SIGKILL bash -c `.
  ✓ Where the host forbids the uid mapping (this host: AppArmor unprivileged_userns profile denies setuid), --user still exits 0 but prints a loud warning that filesystem isolation is NOT active and names the cause; rc semantics for bare mode are unchanged (no silent auto-fallback): `./standalone/terminal-jail --user -- id` on this host prints to stderr: "terminal-jail: WARNING: no filesystem isolation — could not create a uid mapping under the user namespace (no filesystem isolation: this host denies setuid inside unprivileged user namespaces, e.g. Ubuntu AppArmor profile 'unprivileged_userns'; see scripts/fs-isolation-probe.py)" and EXIT=0; inside, TERMINAL_JAIL_FS_ISOLATION=degraded. Host confirms the cause: /proc/sys/kernel/apparmor_restrict_unprivileged_userns=1. Bare mode unchanged: standalone/terminal-jail:365-372 still exits 2 with a message on namespace-creation failure and never auto-adds --user (README.md:250 documents 'no automatic fallback ... never silently downgrades isolation').
  ✓ scripts/fs-isolation-probe.py reports the filesystem-isolation layer honestly (FULL/DEGRADED + cause) and always exits 0; on this host it prints DEGRADED with the uid-mapping cause: `./scripts/fs-isolation-probe.py` -> "DEGRADED: mapped launch failed (rc=1, stderr='unshare: setgroups failed: Operation not permitted'); causes: uid-mapping denied: mapped launch failed while the legacy one succeeded (kernel denies setgid/setgroups inside unprivileged user namespaces — AppArmor profile 'unprivileged_userns') [kernel.apparmor_restrict_unprivileged_userns=1]. remediation: ..." with exit=0. Honesty proven both ways: `sudo -n ./scripts/fs-isolation-probe.py` -> "FULL: uid-mapped user namespace denies caller-owned 600 reads and home writes", exit=0. main() (scripts/fs-isolation-probe.py:194-195) only prints — no nonzero sys.exit path, so it always exits 0.
  ✓ Docs no longer claim uid/filesystem isolation: README, docs/quickstart.md, specs/cli.md and the in-repo skills/terminal-jail-usage/SKILL.md state that --user = PID lifecycle containment + env scrub, and that filesystem isolation requires a working uid mapping: All four files updated in commit f0cc29b. grep for stale claims ("UID isolation", "runs as nobody=65534", "trades /proc isolation for UID") across README.md, docs/quickstart.md, specs/cli.md, skills/terminal-jail-usage/SKILL.md returns ZERO matches. New text present: specs/cli.md:79 and :170 ("--user ... for PID-namespace lifecycle containment + identity env scrub. Filesystem isolation requires a uid mapping (--map-users/--map-groups with -S/-G); it is active ONLY when the exact-flags preflight passes (TERMINAL_JAIL_FS_ISOLATION=mapped)"); README.md:250 ("--user itself has two tiers ... real filesystem isolation, TERMINAL_JAIL_FS_ISOLATION=mapped ... otherwise ... loud `no filesystem isolation` warning"); docs/quickstart.md FAQ "Does --user isolate the filesystem? Only when the host allows a uid mapping"; skills/terminal-jail-usage/SKILL.md item 3 ("--user filesystem isolation is CONDITIONAL (TJ-DF-015, fixed 2026-09-15)").
  ✓ The mapped recipe is proven to actually isolate where the host permits it: the same flags run under sudo yield uid=65534 and EACCES on a caller-owned 600 file; the plugin test suite stays green (no new failures vs 334 passed/13 skipped): Mapped recipe under sudo: `sudo -n unshare --user --map-users=65534:100000:1 --map-groups=65534:100000:1 -S 65534 -G 65534 --pid --fork --kill-child=SIGKILL id` -> `uid=65534(nobody) gid=65534(nogroup)`, EXIT=0; same flags with `cat /tmp/tj015/secret600` (caller-owned, mode 600) -> `cat: /tmp/tj015/secret600: Permission denied`, EXIT=1 (EACCES). Test suite: baseline worktree at a94af73 (/tmp/tjbase) = `334 passed, 13 skipped`; HEAD f0cc29b = `348 passed, 14 skipped in 8.40s` via the configured `pytest -x --tb=short` (exit 0) — delta is exactly the new plugin/test_userns.py (14 passed, 1 skipped), so no new failures. LSP diagnostics: 0 findings.
All five criteria verified with live command evidence: mapped uid launch attempted first with TERMINAL_JAIL_FS_ISOLATION=mapped on success (proven under sudo), loud exit-0 degraded warning naming the AppArmor cause on this host, honest FULL/DEGRADED probe that always exits 0, all four docs corrected with no stale isolation claims, and the mapped recipe proven to yield uid=65534 + EACCES while the suite went 334/13 -> 348/14 with no new failures.

Overall: PASS ✓
