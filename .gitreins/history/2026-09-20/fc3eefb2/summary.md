# Verdict: VERSION-002

**Task:** Load-hygiene: bridge integration tests run in-process, real-exec parity seam kept
**Evaluated:** 2026-09-20T11:53:48.853841
**Result:** ✓ PASS

## Pipeline Stages

- ✓ **tier1**
  -   ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================
- ✓ **tier2**
  - COMPLETE
  ✓ Bridge verdict assertions run in-process: plugin/test_interruptor_integration.py drives interruptor_bridge.main() via _bridge_main_inproc for every bridge-verdict assertion; the file contains exactly 2 real subprocess.run call sites - the _run_cli helper used by CLI-level tests (there the process boundary IS the subject, by design) and the single real spawn inside test_bridge_real_exec_parity, which runs the real python3 interruptor_bridge.py and asserts byte-identical stdout and equal rc against the in-process path across the BRIDGE_PARITY_INPUTS set (allow, block, modify, empty-schema, null). A third grep hit for the literal string subprocess.run is a docstring mention, not a call site.: grep -n 'subprocess.run' plugin/test_interruptor_integration.py yields exactly 3 hits: line 70 (inside _run_cli, real spawn by design), line 329 (inside test_bridge_real_exec_parity, real spawn), line 363 (docstring text only). Before the change (ec3e645~1) there were 6 call sites. All bridge-verdict assertions use _bridge_main_inproc (lines 288, 294, 339, 374, 390, 563, 728). BRIDGE_PARITY_INPUTS (lines 307-313) covers allow ('echo hello'), block ('rm -rf /'), modify (quoted pytest), empty-schema ('{}'), null ('null'). The parity test spawns the real script via sys.executable at line 330 and asserts proc.returncode == inproc.returncode == 0 and proc.stdout == inproc.stdout. Verified the real spawn works: real rc:0 stdout b'{"action": "allow", "command": "echo hello", ...}'.
  ✓ Structural seam test test_bridge_in_process_path_never_spawns_a_process patches subprocess.run to raise and the in-process path still answers allow; it warms the engine import explicitly so isolated and -k runs behave like full-file runs: plugin/test_interruptor_integration.py:352-377: patches subprocess.run with side_effect=AssertionError('spawned a process') and asserts response['action'] == 'allow'. Line 369 warms the engine import via importlib.import_module('terminal_jail.interruptor'). Isolated run: `.venv/bin/python -m pytest plugin/test_interruptor_integration.py::test_bridge_in_process_path_never_spawns_a_process -q` -> '1 passed in 0.04s'. -k run: '1 passed, 37 deselected in 0.04s'. Injection test (adding subprocess.run(['true']) into _bridge_main_inproc) makes it FAIL with 'AssertionError: spawned a process', proving it structurally pins the seam. Fix commit f701200 explicitly added the import warm-up.
  ✓ Measured: file wall time 7.8s before vs 4.1s after; full suite on merged main reports 1018 passed, 7 skipped; zero tests deleted, zero skip marks added (35 to 37 tests in the file, +2 seam tests): Full suite `.venv/bin/python -m pytest plugin -q` -> '1018 passed, 7 skipped in 28.15s' (exact match). Zero tests deleted: comm of sorted test names before (ec3e645~1) vs after shows 0 removed and exactly +2 added (test_bridge_in_process_path_never_spawns_a_process, test_bridge_real_exec_parity). Skip marks: 1 before, 1 after (unchanged pre-existing test_rule_hot_reload T-I40). File wall time after measured 1.98-2.18s across 3 runs (well under 4.1s). Minor imprecision: the criterion states '35 to 37 tests in the file' but actual collected count is 36 before -> 38 after (test defs 23->25); the +2 delta is exact and the prior verdict 2e558c4a recorded the same 36->38. The substantive claims (zero deleted, zero skip marks added, +2 seam tests) all hold.
  ✓ ruff check clean on the changed file (uvx ruff check plugin/test_interruptor_integration.py) and full suite green on merged main at HEAD ec3e645 or later: `uvx ruff check plugin/test_interruptor_integration.py` -> 'All checks passed!' EXIT=0. Full suite `.venv/bin/python -m pytest plugin -q` -> '1018 passed, 7 skipped in 28.15s', exit 0. HEAD = f70120087c1cc36ffe3756afb6bc01bc31f85c67; `git merge-base --is-ancestor ec3e645 HEAD` confirms ec3e645 is an ancestor of HEAD, satisfying 'at HEAD ec3e645 or later'.
  ✓ CHANGELOG.md [Unreleased] carries the VERSION-002 entry with the before/after numbers: CHANGELOG.md line 1 '## [Unreleased]'; line 3 '### Bridge-level integration tests run in-process; one real-exec parity test keeps the wire contract (VERSION-002)'; line 12 contains 'File wall time 7.8s → 4.1s; suite total -3.7s.' The entry describes the in-process conversion, test_bridge_real_exec_parity, and the structural no-spawn guard test.
All 5 criteria pass: bridge assertions run in-process with exactly 2 real subprocess.run call sites plus a docstring mention, the seam test structurally pins no-spawn and passes isolated/-k, full suite reports 1018 passed/7 skipped with zero deletions and +2 seam tests, ruff is clean at HEAD f701200 (descendant of ec3e645), and CHANGELOG carries the VERSION-002 entry with 7.8s→4.1s; only a minor off-by-one in the criterion's stated test count (actual 36→38 vs stated 35→37) which does not affect the +2 delta.

## Summary

Judge Result: VERSION-002

Stage tier1: PASS
    ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================

Stage tier2: PASS
  COMPLETE
  ✓ Bridge verdict assertions run in-process: plugin/test_interruptor_integration.py drives interruptor_bridge.main() via _bridge_main_inproc for every bridge-verdict assertion; the file contains exactly 2 real subprocess.run call sites - the _run_cli helper used by CLI-level tests (there the process boundary IS the subject, by design) and the single real spawn inside test_bridge_real_exec_parity, which runs the real python3 interruptor_bridge.py and asserts byte-identical stdout and equal rc against the in-process path across the BRIDGE_PARITY_INPUTS set (allow, block, modify, empty-schema, null). A third grep hit for the literal string subprocess.run is a docstring mention, not a call site.: grep -n 'subprocess.run' plugin/test_interruptor_integration.py yields exactly 3 hits: line 70 (inside _run_cli, real spawn by design), line 329 (inside test_bridge_real_exec_parity, real spawn), line 363 (docstring text only). Before the change (ec3e645~1) there were 6 call sites. All bridge-verdict assertions use _bridge_main_inproc (lines 288, 294, 339, 374, 390, 563, 728). BRIDGE_PARITY_INPUTS (lines 307-313) covers allow ('echo hello'), block ('rm -rf /'), modify (quoted pytest), empty-schema ('{}'), null ('null'). The parity test spawns the real script via sys.executable at line 330 and asserts proc.returncode == inproc.returncode == 0 and proc.stdout == inproc.stdout. Verified the real spawn works: real rc:0 stdout b'{"action": "allow", "command": "echo hello", ...}'.
  ✓ Structural seam test test_bridge_in_process_path_never_spawns_a_process patches subprocess.run to raise and the in-process path still answers allow; it warms the engine import explicitly so isolated and -k runs behave like full-file runs: plugin/test_interruptor_integration.py:352-377: patches subprocess.run with side_effect=AssertionError('spawned a process') and asserts response['action'] == 'allow'. Line 369 warms the engine import via importlib.import_module('terminal_jail.interruptor'). Isolated run: `.venv/bin/python -m pytest plugin/test_interruptor_integration.py::test_bridge_in_process_path_never_spawns_a_process -q` -> '1 passed in 0.04s'. -k run: '1 passed, 37 deselected in 0.04s'. Injection test (adding subprocess.run(['true']) into _bridge_main_inproc) makes it FAIL with 'AssertionError: spawned a process', proving it structurally pins the seam. Fix commit f701200 explicitly added the import warm-up.
  ✓ Measured: file wall time 7.8s before vs 4.1s after; full suite on merged main reports 1018 passed, 7 skipped; zero tests deleted, zero skip marks added (35 to 37 tests in the file, +2 seam tests): Full suite `.venv/bin/python -m pytest plugin -q` -> '1018 passed, 7 skipped in 28.15s' (exact match). Zero tests deleted: comm of sorted test names before (ec3e645~1) vs after shows 0 removed and exactly +2 added (test_bridge_in_process_path_never_spawns_a_process, test_bridge_real_exec_parity). Skip marks: 1 before, 1 after (unchanged pre-existing test_rule_hot_reload T-I40). File wall time after measured 1.98-2.18s across 3 runs (well under 4.1s). Minor imprecision: the criterion states '35 to 37 tests in the file' but actual collected count is 36 before -> 38 after (test defs 23->25); the +2 delta is exact and the prior verdict 2e558c4a recorded the same 36->38. The substantive claims (zero deleted, zero skip marks added, +2 seam tests) all hold.
  ✓ ruff check clean on the changed file (uvx ruff check plugin/test_interruptor_integration.py) and full suite green on merged main at HEAD ec3e645 or later: `uvx ruff check plugin/test_interruptor_integration.py` -> 'All checks passed!' EXIT=0. Full suite `.venv/bin/python -m pytest plugin -q` -> '1018 passed, 7 skipped in 28.15s', exit 0. HEAD = f70120087c1cc36ffe3756afb6bc01bc31f85c67; `git merge-base --is-ancestor ec3e645 HEAD` confirms ec3e645 is an ancestor of HEAD, satisfying 'at HEAD ec3e645 or later'.
  ✓ CHANGELOG.md [Unreleased] carries the VERSION-002 entry with the before/after numbers: CHANGELOG.md line 1 '## [Unreleased]'; line 3 '### Bridge-level integration tests run in-process; one real-exec parity test keeps the wire contract (VERSION-002)'; line 12 contains 'File wall time 7.8s → 4.1s; suite total -3.7s.' The entry describes the in-process conversion, test_bridge_real_exec_parity, and the structural no-spawn guard test.
All 5 criteria pass: bridge assertions run in-process with exactly 2 real subprocess.run call sites plus a docstring mention, the seam test structurally pins no-spawn and passes isolated/-k, full suite reports 1018 passed/7 skipped with zero deletions and +2 seam tests, ruff is clean at HEAD f701200 (descendant of ec3e645), and CHANGELOG carries the VERSION-002 entry with 7.8s→4.1s; only a minor off-by-one in the criterion's stated test count (actual 36→38 vs stated 35→37) which does not affect the +2 delta.

Overall: PASS ✓
