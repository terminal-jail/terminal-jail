# Verdict: VERSION-002

**Task:** Load-hygiene: bridge integration tests run in-process, real-exec parity seam kept
**Evaluated:** 2026-09-20T10:23:52.670925
**Result:** ✗ FAIL

## Pipeline Stages

- ✓ **tier1**
  -   ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================
- ✗ **tier2**
  - INCOMPLETE
  ✓ In-process conversion: plugin/test_interruptor_integration.py drives interruptor_bridge.main() via _bridge_main_inproc for bridge verdict assertions; grep shows exactly 3 subprocess.run sites remaining, all inside test_bridge_real_exec_parity which spawns the real python3 interruptor_bridge.py and asserts byte-identical stdout and rc against the in-process path across allow/block/modify/empty-schema/null inputs: grep -c subprocess.run = 3 (lines 70, 329, 357-comment). Line 329 is inside test_bridge_real_exec_parity (line 317), which spawns [sys.executable, str(BRIDGE_SCRIPT)] and asserts proc.returncode == inproc.returncode == 0 and proc.stdout == inproc.stdout. BRIDGE_PARITY_INPUTS (line 307) = allow/block/modify/{}/null; I ran each through _bridge_main_inproc and confirmed verdicts allow, block, modify, allow([bridge-error] missing 'command' key), allow([bridge-error] payload must be a JSON object). _bridge_main_inproc defined at line 85 and used throughout.
  ✗ Structural seam test test_bridge_in_process_path_never_spawns_a_process patches subprocess.run to raise and the in-process path still answers allow: The test exists (line 352) and passes in the canonical full-file run (37 passed) and full suite (1018 passed), but it FAILS whenever the engine module is not already imported: `pytest plugin/test_interruptor_integration.py::test_bridge_in_process_path_never_spawns_a_process` -> 1 failed (AssertionError: spawned a process), and `-k never_spawns` -> 1 failed, 37 deselected. Root cause: decider.py:36 `_UNSHARE_PREFIX = unshare_prefix()` calls subprocess.run at MODULE IMPORT time (userns.py:117), so the test's mock.patch.object(subprocess,'run') intercepts that import-time probe unless terminal_jail.interruptor is already in sys.modules. It passes only by import-caching accident from earlier tests, so it does not structurally pin the seam as its docstring claims — a future edit reintroducing a spawn would not be caught when the test runs in isolation or under a -k filter.
  ✓ Measured: file wall time 7.8s before vs 4.1s after (main checkout baseline re-measured this tick); full suite on merged main reports 1018 passed, 7 skipped; zero tests deleted, zero skip marks added (35 to 37 tests in the file, +2 seam tests): Re-measured: pre-change file (git show 6f6c2e6^) = 35 passed, 1 skipped in 7.48s; current file = 37 passed, 1 skipped in 3.98-4.12s (matches 7.8s -> 4.1s). Collected 36 before -> 38 after. Test defs 23 -> 25; diff of sorted test names shows exactly +2 (test_bridge_in_process_path_never_spawns_a_process, test_bridge_real_exec_parity) and zero deletions. pytest.mark.skip count: 1 before, 1 after (unchanged). Full suite: `.venv/bin/python -m pytest plugin -q` -> 1018 passed, 7 skipped in 35.35s.
  ✓ ruff check clean on the changed file (uvx ruff check plugin/test_interruptor_integration.py) and full suite green on merged main at HEAD ec3e645: `uvx ruff check plugin/test_interruptor_integration.py` -> 'All checks passed!' EXIT=0. HEAD = ec3e645b14d23e5060c245b4c62a01e32d1cfd31 (matches). Full suite `.venv/bin/python -m pytest plugin -q` -> 1018 passed, 7 skipped in 35.35s, exit 0.
  ✓ CHANGELOG.md [Unreleased] carries the VERSION-002 entry with the before/after numbers: CHANGELOG.md lines 1-13: [Unreleased] first section '### Bridge-level integration tests run in-process; one real-exec parity test keeps the wire contract (VERSION-002)' contains 'File wall time 7.8s → 4.1s; suite total -3.7s' and describes the in-process conversion plus test_bridge_real_exec_parity and the structural no-spawn guard test.
Four criteria pass with strong measured evidence, but criterion 2 fails: the seam test only passes by import-caching accident and fails in isolation and under -k filtering, so it does not structurally pin the no-spawn seam as claimed.

## Summary

Judge Result: VERSION-002

Stage tier1: PASS
    ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================

Stage tier2: FAIL
  INCOMPLETE
  ✓ In-process conversion: plugin/test_interruptor_integration.py drives interruptor_bridge.main() via _bridge_main_inproc for bridge verdict assertions; grep shows exactly 3 subprocess.run sites remaining, all inside test_bridge_real_exec_parity which spawns the real python3 interruptor_bridge.py and asserts byte-identical stdout and rc against the in-process path across allow/block/modify/empty-schema/null inputs: grep -c subprocess.run = 3 (lines 70, 329, 357-comment). Line 329 is inside test_bridge_real_exec_parity (line 317), which spawns [sys.executable, str(BRIDGE_SCRIPT)] and asserts proc.returncode == inproc.returncode == 0 and proc.stdout == inproc.stdout. BRIDGE_PARITY_INPUTS (line 307) = allow/block/modify/{}/null; I ran each through _bridge_main_inproc and confirmed verdicts allow, block, modify, allow([bridge-error] missing 'command' key), allow([bridge-error] payload must be a JSON object). _bridge_main_inproc defined at line 85 and used throughout.
  ✗ Structural seam test test_bridge_in_process_path_never_spawns_a_process patches subprocess.run to raise and the in-process path still answers allow: The test exists (line 352) and passes in the canonical full-file run (37 passed) and full suite (1018 passed), but it FAILS whenever the engine module is not already imported: `pytest plugin/test_interruptor_integration.py::test_bridge_in_process_path_never_spawns_a_process` -> 1 failed (AssertionError: spawned a process), and `-k never_spawns` -> 1 failed, 37 deselected. Root cause: decider.py:36 `_UNSHARE_PREFIX = unshare_prefix()` calls subprocess.run at MODULE IMPORT time (userns.py:117), so the test's mock.patch.object(subprocess,'run') intercepts that import-time probe unless terminal_jail.interruptor is already in sys.modules. It passes only by import-caching accident from earlier tests, so it does not structurally pin the seam as its docstring claims — a future edit reintroducing a spawn would not be caught when the test runs in isolation or under a -k filter.
  ✓ Measured: file wall time 7.8s before vs 4.1s after (main checkout baseline re-measured this tick); full suite on merged main reports 1018 passed, 7 skipped; zero tests deleted, zero skip marks added (35 to 37 tests in the file, +2 seam tests): Re-measured: pre-change file (git show 6f6c2e6^) = 35 passed, 1 skipped in 7.48s; current file = 37 passed, 1 skipped in 3.98-4.12s (matches 7.8s -> 4.1s). Collected 36 before -> 38 after. Test defs 23 -> 25; diff of sorted test names shows exactly +2 (test_bridge_in_process_path_never_spawns_a_process, test_bridge_real_exec_parity) and zero deletions. pytest.mark.skip count: 1 before, 1 after (unchanged). Full suite: `.venv/bin/python -m pytest plugin -q` -> 1018 passed, 7 skipped in 35.35s.
  ✓ ruff check clean on the changed file (uvx ruff check plugin/test_interruptor_integration.py) and full suite green on merged main at HEAD ec3e645: `uvx ruff check plugin/test_interruptor_integration.py` -> 'All checks passed!' EXIT=0. HEAD = ec3e645b14d23e5060c245b4c62a01e32d1cfd31 (matches). Full suite `.venv/bin/python -m pytest plugin -q` -> 1018 passed, 7 skipped in 35.35s, exit 0.
  ✓ CHANGELOG.md [Unreleased] carries the VERSION-002 entry with the before/after numbers: CHANGELOG.md lines 1-13: [Unreleased] first section '### Bridge-level integration tests run in-process; one real-exec parity test keeps the wire contract (VERSION-002)' contains 'File wall time 7.8s → 4.1s; suite total -3.7s' and describes the in-process conversion plus test_bridge_real_exec_parity and the structural no-spawn guard test.
Four criteria pass with strong measured evidence, but criterion 2 fails: the seam test only passes by import-caching accident and fails in isolation and under -k filtering, so it does not structurally pin the no-spawn seam as claimed.

Overall: FAIL ✗
