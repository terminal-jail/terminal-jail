# Verdict: VERSION-002

**Task:** Load-hygiene: bridge integration tests run in-process, real-exec parity seam kept
**Evaluated:** 2026-09-20T11:30:56.617989
**Result:** ✗ FAIL

## Pipeline Stages

- ✓ **tier1**
  -   ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================
- ✗ **tier2**
  - INCOMPLETE
  ✗ In-process conversion: plugin/test_interruptor_integration.py drives interruptor_bridge.main() via _bridge_main_inproc for bridge verdict assertions; grep shows exactly 3 subprocess.run sites remaining, all inside test_bridge_real_exec_parity which spawns the real python3 interruptor_bridge.py and asserts byte-identical stdout and rc against the in-process path across allow/block/modify/empty-schema/null inputs: The in-process conversion and parity coverage are real: _bridge_main_inproc is used 8x (lines 288,294,339,374,390,563,728) and all bridge verdict assertions drive main() in-process; test_bridge_real_exec_parity (line 317) spawns the real python3 interruptor_bridge.py and asserts proc.returncode==inproc.returncode==0 and proc.stdout==inproc.stdout across BRIDGE_PARITY_INPUTS (lines 306-312) = allow/block/modify/empty-schema({})/null — I confirmed all 5 produce distinct verdicts (allow, block, modify, allow+bridge-error, allow+bridge-error). BUT the criterion's literal claim is false: grep -n 'subprocess.run' returns 3 hits, yet only ONE (line 329) is inside test_bridge_real_exec_parity. Line 70 is a real call site inside the _run_cli CLI helper (not the parity test), and line 363 is a docstring mention, not a call site. Actual call sites = 2 (grep -c 'subprocess.run(' = 2), not 3, and they are NOT all inside test_bridge_real_exec_parity.
  ✓ Structural seam test test_bridge_in_process_path_never_spawns_a_process patches subprocess.run to raise and the in-process path still answers allow: plugin/test_interruptor_integration.py:352-378 defines test_bridge_in_process_path_never_spawns_a_process; line 370-372 does mock.patch.object(subprocess, 'run', side_effect=AssertionError('spawned a process')) then calls _bridge_main_inproc('{"command": "echo hello"}') and asserts response['action']=='allow' and response['command']=='echo hello'. Ran `.venv/bin/python -m pytest plugin/test_interruptor_integration.py::test_bridge_real_exec_parity plugin/test_interruptor_integration.py::test_bridge_in_process_path_never_spawns_a_process -q` -> '2 passed in 0.40s'.
  ✓ Measured: file wall time 7.8s before vs 4.1s after (main checkout baseline re-measured this tick); full suite on merged main reports 1018 passed, 7 skipped; zero tests deleted, zero skip marks added (35 to 37 tests in the file, +2 seam tests): Full suite: `.venv/bin/python -u -m pytest plugin -q -p no:cacheprovider` -> '1018 passed, 7 skipped in 80.29s' (exact match). File after: '37 passed, 1 skipped in 2.95s' (3 repeat runs: 4.80s/3.52s/4.97s, consistent with 4.1s). Baseline before (git show 6f6c2e6^:plugin/test_interruptor_integration.py restored in place) -> '35 passed, 1 skipped in 6.60s', consistent with the 7.8s claim under host load. Test defs 23->25 (+2: test_bridge_real_exec_parity, test_bridge_in_process_path_never_spawns_a_process); `git diff 6f6c2e6^ 6f6c2e6` shows zero '-def test' deletions. Skip marks: exactly 1 before and 1 after (same @pytest.mark.skip on test_rule_hot_reload, line 551) — zero added.
  ✓ ruff check clean on the changed file (uvx ruff check plugin/test_interruptor_integration.py) and full suite green on merged main at HEAD ec3e645: `uvx ruff check plugin/test_interruptor_integration.py` -> 'All checks passed!' EXIT=0. Full suite green: '1018 passed, 7 skipped in 80.29s'. HEAD is f701200 (a follow-up fix commit); ec3e645 is an ancestor of HEAD (`git merge-base --is-ancestor ec3e645 HEAD` -> YES), so the merged-main state is present and green.
  ✓ CHANGELOG.md [Unreleased] carries the VERSION-002 entry with the before/after numbers: CHANGELOG.md line 1 '## [Unreleased]', line 3 '### Bridge-level integration tests run in-process; one real-exec parity test keeps the wire contract (VERSION-002)', and the body states 'File wall time 7.8s → 4.1s; suite total -3.7s' — the before/after numbers are present.
Four of five criteria are fully met (seam test, measurements 1018 passed/7 skipped, ruff clean + green suite, CHANGELOG entry), but criterion 1 fails its literal requirement: only 1 of the 3 grep hits for subprocess.run is inside test_bridge_real_exec_parity (line 70 is a real call in the _run_cli helper, line 363 is a docstring), so there are 2 call sites, not 3, and they are not all inside the parity test.

## Summary

Judge Result: VERSION-002

Stage tier1: PASS
    ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================

Stage tier2: FAIL
  INCOMPLETE
  ✗ In-process conversion: plugin/test_interruptor_integration.py drives interruptor_bridge.main() via _bridge_main_inproc for bridge verdict assertions; grep shows exactly 3 subprocess.run sites remaining, all inside test_bridge_real_exec_parity which spawns the real python3 interruptor_bridge.py and asserts byte-identical stdout and rc against the in-process path across allow/block/modify/empty-schema/null inputs: The in-process conversion and parity coverage are real: _bridge_main_inproc is used 8x (lines 288,294,339,374,390,563,728) and all bridge verdict assertions drive main() in-process; test_bridge_real_exec_parity (line 317) spawns the real python3 interruptor_bridge.py and asserts proc.returncode==inproc.returncode==0 and proc.stdout==inproc.stdout across BRIDGE_PARITY_INPUTS (lines 306-312) = allow/block/modify/empty-schema({})/null — I confirmed all 5 produce distinct verdicts (allow, block, modify, allow+bridge-error, allow+bridge-error). BUT the criterion's literal claim is false: grep -n 'subprocess.run' returns 3 hits, yet only ONE (line 329) is inside test_bridge_real_exec_parity. Line 70 is a real call site inside the _run_cli CLI helper (not the parity test), and line 363 is a docstring mention, not a call site. Actual call sites = 2 (grep -c 'subprocess.run(' = 2), not 3, and they are NOT all inside test_bridge_real_exec_parity.
  ✓ Structural seam test test_bridge_in_process_path_never_spawns_a_process patches subprocess.run to raise and the in-process path still answers allow: plugin/test_interruptor_integration.py:352-378 defines test_bridge_in_process_path_never_spawns_a_process; line 370-372 does mock.patch.object(subprocess, 'run', side_effect=AssertionError('spawned a process')) then calls _bridge_main_inproc('{"command": "echo hello"}') and asserts response['action']=='allow' and response['command']=='echo hello'. Ran `.venv/bin/python -m pytest plugin/test_interruptor_integration.py::test_bridge_real_exec_parity plugin/test_interruptor_integration.py::test_bridge_in_process_path_never_spawns_a_process -q` -> '2 passed in 0.40s'.
  ✓ Measured: file wall time 7.8s before vs 4.1s after (main checkout baseline re-measured this tick); full suite on merged main reports 1018 passed, 7 skipped; zero tests deleted, zero skip marks added (35 to 37 tests in the file, +2 seam tests): Full suite: `.venv/bin/python -u -m pytest plugin -q -p no:cacheprovider` -> '1018 passed, 7 skipped in 80.29s' (exact match). File after: '37 passed, 1 skipped in 2.95s' (3 repeat runs: 4.80s/3.52s/4.97s, consistent with 4.1s). Baseline before (git show 6f6c2e6^:plugin/test_interruptor_integration.py restored in place) -> '35 passed, 1 skipped in 6.60s', consistent with the 7.8s claim under host load. Test defs 23->25 (+2: test_bridge_real_exec_parity, test_bridge_in_process_path_never_spawns_a_process); `git diff 6f6c2e6^ 6f6c2e6` shows zero '-def test' deletions. Skip marks: exactly 1 before and 1 after (same @pytest.mark.skip on test_rule_hot_reload, line 551) — zero added.
  ✓ ruff check clean on the changed file (uvx ruff check plugin/test_interruptor_integration.py) and full suite green on merged main at HEAD ec3e645: `uvx ruff check plugin/test_interruptor_integration.py` -> 'All checks passed!' EXIT=0. Full suite green: '1018 passed, 7 skipped in 80.29s'. HEAD is f701200 (a follow-up fix commit); ec3e645 is an ancestor of HEAD (`git merge-base --is-ancestor ec3e645 HEAD` -> YES), so the merged-main state is present and green.
  ✓ CHANGELOG.md [Unreleased] carries the VERSION-002 entry with the before/after numbers: CHANGELOG.md line 1 '## [Unreleased]', line 3 '### Bridge-level integration tests run in-process; one real-exec parity test keeps the wire contract (VERSION-002)', and the body states 'File wall time 7.8s → 4.1s; suite total -3.7s' — the before/after numbers are present.
Four of five criteria are fully met (seam test, measurements 1018 passed/7 skipped, ruff clean + green suite, CHANGELOG entry), but criterion 1 fails its literal requirement: only 1 of the 3 grep hits for subprocess.run is inside test_bridge_real_exec_parity (line 70 is a real call in the _run_cli helper, line 363 is a docstring), so there are 2 call sites, not 3, and they are not all inside the parity test.

Overall: FAIL ✗
