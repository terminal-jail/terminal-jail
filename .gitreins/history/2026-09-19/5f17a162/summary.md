# Verdict: DF-TERMINAL-JAIL-15

**Task:** Transparent auto-sandbox must not rewrite commands into a launch that cannot read the caller's files
**Evaluated:** 2026-09-19T00:03:43.328986
**Result:** ✓ PASS

## Pipeline Stages

- ✓ **tier1**
  -   ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================
- ✓ **tier2**
  - COMPLETE
  ✓ userns.unshare_prefix() selects a launch that passed a runtime file-access property preflight (payload reads a caller-owned mode-600 probe file AND writes in the caller's cwd), not merely namespace creation: userns.py unshare_prefix() (lines 340-363) returns the mapped prefix only when `observed == _PROPERTY_MARKER` ('read_rc=0 write_rc=0'), where observed = _property_observation() run after mapped_launch_ok(). _property_payload (line ~211) does `cat <secret> >/dev/null` on a caller-created mode-600 file (os.chmod(secret, 0o600), line ~250) AND `echo ... > <target>` where target is in the caller's cwd (workdir=os.getcwd()). Both halves required; timeout/OSError/non-zero exit fail closed. Namespace creation alone (mapped_launch_ok) is explicitly insufficient.
  ✓ plugin/test_userns.py covers both branches (ns-creation OK + property FAIL -> legacy prefix + loud warning; property OK -> mapped prefix) and fails on the pre-fix code: TestFileAccessPreflight::test_capable_host_without_file_access_falls_back_to_legacy (ns OK + property FAIL -> legacy prefix, exactly ONE 'no filesystem isolation' warning naming TERMINAL_JAIL_UID_MAP=0 and DF-TERMINAL-JAIL-15) and test_property_pass_selects_the_mapped_prefix (property OK -> mapped prefix, no warning). RED-proven: swapping in pre-fix userns.py (e22a3c1^) makes all 11 TestFileAccessPreflight tests FAIL — behavioural core fails by assertion ('unshare --user --map-users=...' returned on creation alone). Restored cleanly (git status clean).
  ✓ Explicit hard-isolation path unchanged: TERMINAL_JAIL_UID_MAP=0|off|false forces legacy; standalone/terminal-jail --user mapped preflight and scripts/fs-isolation-probe.py semantics intact: Engine uid_map_disabled() (userns.py:128-134) checked at line 353; wrapper standalone/terminal-jail case 0|off|false -> legacy (lines 361-365). `git show --stat e22a3c1 9048370 -- standalone/terminal-jail scripts/fs-isolation-probe.py` is empty — neither file modified, so the --user creation-only preflight and probe semantics are intact. scripts/fs-isolation-probe.py runs and exits 0 (prints DEGRADED + cause). Tests test_uid_map_disabled_env, test_unshare_prefix_forced_off, test_uid_map_off_forces_legacy_with_warning, TestDegradedFallback pass.
  ✓ Suite green on the final tree: .venv/bin/python -m pytest -q (pre-fix baseline 588 passed / 5 skipped) plus uvx ruff check . -> All checks passed: `.venv/bin/python -m pytest -q` -> exit 0, '601 passed, 5 skipped in 13.46s' (baseline 588 passed/5 skipped; +13 new tests). `uvx ruff check .` -> exit 0, 'All checks passed!'. LSP diagnostics: 0 findings.
  ✓ README.md + specs/cli.md + CHANGELOG.md state the new selection rule and do not claim filesystem isolation for transparent auto-sandbox: README.md:158-159 states the rule (mapped only when host can create it AND payload can read caller mode-600 file + write in caller cwd) and 'It never claims filesystem isolation it does not have: the mapped launch stays the explicit hard-isolation path'. specs/cli.md new bullet 'Transparent auto-sandbox / modify wrap selection' states the rule and 'The auto-sandbox therefore makes no filesystem-isolation claim'. CHANGELOG.md Unreleased section states the rule and 'neither claims filesystem isolation for the transparent auto-sandbox'. specs/interruptor.md stale creation-only contract also corrected.
All 5 criteria pass: unshare_prefix() now selects the mapped launch only on a proven file-access property preflight, tests cover both branches and are RED-proven against pre-fix code, the explicit --user path is untouched, the suite is green (601 passed/5 skipped, ruff clean), and all docs state the new rule without claiming filesystem isolation for the transparent auto-sandbox.

## Summary

Judge Result: DF-TERMINAL-JAIL-15

Stage tier1: PASS
    ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================

Stage tier2: PASS
  COMPLETE
  ✓ userns.unshare_prefix() selects a launch that passed a runtime file-access property preflight (payload reads a caller-owned mode-600 probe file AND writes in the caller's cwd), not merely namespace creation: userns.py unshare_prefix() (lines 340-363) returns the mapped prefix only when `observed == _PROPERTY_MARKER` ('read_rc=0 write_rc=0'), where observed = _property_observation() run after mapped_launch_ok(). _property_payload (line ~211) does `cat <secret> >/dev/null` on a caller-created mode-600 file (os.chmod(secret, 0o600), line ~250) AND `echo ... > <target>` where target is in the caller's cwd (workdir=os.getcwd()). Both halves required; timeout/OSError/non-zero exit fail closed. Namespace creation alone (mapped_launch_ok) is explicitly insufficient.
  ✓ plugin/test_userns.py covers both branches (ns-creation OK + property FAIL -> legacy prefix + loud warning; property OK -> mapped prefix) and fails on the pre-fix code: TestFileAccessPreflight::test_capable_host_without_file_access_falls_back_to_legacy (ns OK + property FAIL -> legacy prefix, exactly ONE 'no filesystem isolation' warning naming TERMINAL_JAIL_UID_MAP=0 and DF-TERMINAL-JAIL-15) and test_property_pass_selects_the_mapped_prefix (property OK -> mapped prefix, no warning). RED-proven: swapping in pre-fix userns.py (e22a3c1^) makes all 11 TestFileAccessPreflight tests FAIL — behavioural core fails by assertion ('unshare --user --map-users=...' returned on creation alone). Restored cleanly (git status clean).
  ✓ Explicit hard-isolation path unchanged: TERMINAL_JAIL_UID_MAP=0|off|false forces legacy; standalone/terminal-jail --user mapped preflight and scripts/fs-isolation-probe.py semantics intact: Engine uid_map_disabled() (userns.py:128-134) checked at line 353; wrapper standalone/terminal-jail case 0|off|false -> legacy (lines 361-365). `git show --stat e22a3c1 9048370 -- standalone/terminal-jail scripts/fs-isolation-probe.py` is empty — neither file modified, so the --user creation-only preflight and probe semantics are intact. scripts/fs-isolation-probe.py runs and exits 0 (prints DEGRADED + cause). Tests test_uid_map_disabled_env, test_unshare_prefix_forced_off, test_uid_map_off_forces_legacy_with_warning, TestDegradedFallback pass.
  ✓ Suite green on the final tree: .venv/bin/python -m pytest -q (pre-fix baseline 588 passed / 5 skipped) plus uvx ruff check . -> All checks passed: `.venv/bin/python -m pytest -q` -> exit 0, '601 passed, 5 skipped in 13.46s' (baseline 588 passed/5 skipped; +13 new tests). `uvx ruff check .` -> exit 0, 'All checks passed!'. LSP diagnostics: 0 findings.
  ✓ README.md + specs/cli.md + CHANGELOG.md state the new selection rule and do not claim filesystem isolation for transparent auto-sandbox: README.md:158-159 states the rule (mapped only when host can create it AND payload can read caller mode-600 file + write in caller cwd) and 'It never claims filesystem isolation it does not have: the mapped launch stays the explicit hard-isolation path'. specs/cli.md new bullet 'Transparent auto-sandbox / modify wrap selection' states the rule and 'The auto-sandbox therefore makes no filesystem-isolation claim'. CHANGELOG.md Unreleased section states the rule and 'neither claims filesystem isolation for the transparent auto-sandbox'. specs/interruptor.md stale creation-only contract also corrected.
All 5 criteria pass: unshare_prefix() now selects the mapped launch only on a proven file-access property preflight, tests cover both branches and are RED-proven against pre-fix code, the explicit --user path is untouched, the suite is green (601 passed/5 skipped, ruff clean), and all docs state the new rule without claiming filesystem isolation for the transparent auto-sandbox.

Overall: PASS ✓
