# Verdict: TJ-GAP-070

**Task:** Fail closed on engine-evaluation errors in interruptor bridge + wrapper verdict hardening
**Evaluated:** 2026-09-21T22:21:18.397396
**Result:** ✗ FAIL

## Pipeline Stages

- ✓ **tier1**
  -   ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================
- ✗ **tier2**
  - INCOMPLETE
  ✗ Poison-priority rules fixture makes standalone/terminal-jail exit 126 with a block box in enforce mode; bridge emits action=block with [bridge-error] reason on intercept() exception; non-JSON/empty bridge stdout blocks in enforce mode; [bridge-error] reason treated as block in enforce, loud warn in warn mode; load-time schema pass refuses bad field types with stderr note; all existing suites stay green; clean-rules control unchanged; regression tests added: MOST sub-claims PASS, but 'empty bridge stdout blocks in enforce mode' is NOT met. LIVE REPRO: a bridge that reads stdin, exits 0 and writes nothing (/tmp/tj070/empty2.py) run as `TERMINAL_JAIL_BRIDGE=/tmp/tj070/empty2.py USE_INTERRUPTOR=1 TERMINAL_JAIL_INTERRUPTOR_MODE=enforce ./standalone/terminal-jail echo PROOF-EMPTY-ENFORCE-RAN` prints PROOF-EMPTY-ENFORCE-RAN and exits RC=0 — the command runs UNGUARDED in enforce mode. Whitespace-only stdout behaves identically (RC=0). ROOT CAUSE: standalone/terminal-jail:208 `python3 "$bridge" 2>/dev/null || return 1` yields an empty command substitution, so `bridge_result=""`; the new fail-closed branch is guarded by `if [ -n "${bridge_result:-}" ]` at line 311 and is therefore SKIPPED ENTIRELY (bash -x trace: `+ bridge_result=` then `+ [ -n '' ]` -> false). The shipped docs promise the opposite: specs/interruptor.md:159 'Bridge stdout empty or not a JSON object ... block, exit 126 (interruptor-verdict-unusable)', README.md:124, docs/quickstart.md:417-418. TEST GAP: plugin/test_bridge_fail_closed.py:13 claims 'empty or non-JSON bridge stdout blocks in enforce mode too', but no test exercises empty stdout (grep for 'empty stdout'/'no usable verdict' in plugin/*.py returns nothing); only test_garbage_bridge_stdout_blocks_in_enforce (non-JSON) exists. VERIFIED PASSING sub-claims: (a) poison fixture enforce -> RC=126 with block box 'COMMAND BLOCKED — [bridge-error]' naming bad.yaml; (b) poison fixture warn -> RC=0 + loud 'WARNING — interruptor bridge reported an error; running the command UNGUARDED (warn mode)'; (c) bridge direct on poison -> {"action":"block","rule_id":"[bridge-error]","reason":"[bridge-error] RuleSchemaError: ... fail-closed: blocking command (enforce mode)"}; (d) non-JSON stdout enforce -> RC=126 'COMMAND BLOCKED — interruptor-verdict-unusable', warn -> loud WARNING + RC=0; (e) allow-shaped [bridge-error] verdict enforce -> RC=126 block box, warn -> loud WARNING + RC=0; (f) loader schema stderr one-line note 'terminal-jail: REFUSING rule file .../bad.yaml: rule #1 (p1): field priority must be an integer, got str (\'not-a-number\')'; (g) clean-rules control: `echo tj070-clean-ran` RC=0, `fdisk -l` RC=126 builtin-fdisk box; (h) focused regression `.venv/bin/python -m pytest plugin/test_bridge_fail_closed.py -q` -> 22 passed in 0.70s; (i) FULL SUITE `.venv/bin/python -m pytest -q -p no:cacheprovider` -> 1392 passed, 7 skipped in 34.31s; (j) `.venv/bin/python -m ruff check` on the 3 changed py files -> 'All checks passed!'; LSP diagnostics empty.
Fail-closed hardening works for engine exceptions, [bridge-error] reasons, non-JSON stdout, schema refusal and clean-rules control (full suite 1392 passed/7 skipped, ruff clean), but the criterion's 'empty bridge stdout blocks in enforce mode' is unmet — an empty/whitespace-only bridge stdout runs the command unguarded with rc 0 in enforce mode because the fail-closed branch is gated behind `[ -n "$bridge_result" ]` (standalone/terminal-jail:311), contradicting specs/interruptor.md:159 and untested by the new regression file.

## Summary

Judge Result: TJ-GAP-070

Stage tier1: PASS
    ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================

Stage tier2: FAIL
  INCOMPLETE
  ✗ Poison-priority rules fixture makes standalone/terminal-jail exit 126 with a block box in enforce mode; bridge emits action=block with [bridge-error] reason on intercept() exception; non-JSON/empty bridge stdout blocks in enforce mode; [bridge-error] reason treated as block in enforce, loud warn in warn mode; load-time schema pass refuses bad field types with stderr note; all existing suites stay green; clean-rules control unchanged; regression tests added: MOST sub-claims PASS, but 'empty bridge stdout blocks in enforce mode' is NOT met. LIVE REPRO: a bridge that reads stdin, exits 0 and writes nothing (/tmp/tj070/empty2.py) run as `TERMINAL_JAIL_BRIDGE=/tmp/tj070/empty2.py USE_INTERRUPTOR=1 TERMINAL_JAIL_INTERRUPTOR_MODE=enforce ./standalone/terminal-jail echo PROOF-EMPTY-ENFORCE-RAN` prints PROOF-EMPTY-ENFORCE-RAN and exits RC=0 — the command runs UNGUARDED in enforce mode. Whitespace-only stdout behaves identically (RC=0). ROOT CAUSE: standalone/terminal-jail:208 `python3 "$bridge" 2>/dev/null || return 1` yields an empty command substitution, so `bridge_result=""`; the new fail-closed branch is guarded by `if [ -n "${bridge_result:-}" ]` at line 311 and is therefore SKIPPED ENTIRELY (bash -x trace: `+ bridge_result=` then `+ [ -n '' ]` -> false). The shipped docs promise the opposite: specs/interruptor.md:159 'Bridge stdout empty or not a JSON object ... block, exit 126 (interruptor-verdict-unusable)', README.md:124, docs/quickstart.md:417-418. TEST GAP: plugin/test_bridge_fail_closed.py:13 claims 'empty or non-JSON bridge stdout blocks in enforce mode too', but no test exercises empty stdout (grep for 'empty stdout'/'no usable verdict' in plugin/*.py returns nothing); only test_garbage_bridge_stdout_blocks_in_enforce (non-JSON) exists. VERIFIED PASSING sub-claims: (a) poison fixture enforce -> RC=126 with block box 'COMMAND BLOCKED — [bridge-error]' naming bad.yaml; (b) poison fixture warn -> RC=0 + loud 'WARNING — interruptor bridge reported an error; running the command UNGUARDED (warn mode)'; (c) bridge direct on poison -> {"action":"block","rule_id":"[bridge-error]","reason":"[bridge-error] RuleSchemaError: ... fail-closed: blocking command (enforce mode)"}; (d) non-JSON stdout enforce -> RC=126 'COMMAND BLOCKED — interruptor-verdict-unusable', warn -> loud WARNING + RC=0; (e) allow-shaped [bridge-error] verdict enforce -> RC=126 block box, warn -> loud WARNING + RC=0; (f) loader schema stderr one-line note 'terminal-jail: REFUSING rule file .../bad.yaml: rule #1 (p1): field priority must be an integer, got str (\'not-a-number\')'; (g) clean-rules control: `echo tj070-clean-ran` RC=0, `fdisk -l` RC=126 builtin-fdisk box; (h) focused regression `.venv/bin/python -m pytest plugin/test_bridge_fail_closed.py -q` -> 22 passed in 0.70s; (i) FULL SUITE `.venv/bin/python -m pytest -q -p no:cacheprovider` -> 1392 passed, 7 skipped in 34.31s; (j) `.venv/bin/python -m ruff check` on the 3 changed py files -> 'All checks passed!'; LSP diagnostics empty.
Fail-closed hardening works for engine exceptions, [bridge-error] reasons, non-JSON stdout, schema refusal and clean-rules control (full suite 1392 passed/7 skipped, ruff clean), but the criterion's 'empty bridge stdout blocks in enforce mode' is unmet — an empty/whitespace-only bridge stdout runs the command unguarded with rc 0 in enforce mode because the fail-closed branch is gated behind `[ -n "$bridge_result" ]` (standalone/terminal-jail:311), contradicting specs/interruptor.md:159 and untested by the new regression file.

Overall: FAIL ✗
