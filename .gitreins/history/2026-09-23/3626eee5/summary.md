# Verdict: TJ-DF-019

**Task:** TJ-DF-019 fix: --user --seccomp must never die on an unreadable loader path
**Evaluated:** 2026-09-23T14:37:13.959249
**Result:** ✓ PASS

## Pipeline Stages

- ✓ **tier1**
  -   ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================
- ✓ **tier2**
  - COMPLETE
  ✓ the wrapper probes whether the seccomp loader path is readable inside the candidate uid-mapped launch, and when it is not, falls back to the mapping-less --user launch while printing an explicit degradation warning that names the 0700-home traversal cause and the path it could not read, and the command still runs with seccomp applied: standalone/terminal-jail:537-563: when USE_SECCOMP=1 and SECCOMP_LOADER is set, the wrapper runs `unshare $_TJ_MAPPED_FLAGS test -r "$SECCOMP_LOADER"` (line 546); on failure it sets _tj_read_failed=1 and prints at line 560: "WARNING: seccomp loader not readable under the uid-mapped launch — falling back to a mapping-less --user launch (could not read '${SECCOMP_LOADER}' as the subordinate uid: a home/permission path such as a 0700 or 750 home is not traversable by it; ...)" then sets UNSHARE_FLAGS="--user $UNSHARE_FLAGS" (line 566). Live run: `TERMINAL_JAIL_SECCOMP_READ_PROBE=false TERMINAL_JAIL_SECCOMP_LOADER=$PWD/standalone/seccomp-loader.py ./standalone/terminal-jail --user --seccomp sh -c 'echo TJDF019-RAN; grep Seccomp /proc/self/status'` -> rc=0, stdout 'TJDF019-RAN' + 'Seccomp:	2' + 'Seccomp_filters:	1', stderr contains the warning naming the exact path and the 0700/750-home cause; TERMINAL_JAIL_FS_ISOLATION=degraded. bash -n standalone/terminal-jail = SYNTAX OK.
  ✓ a test in plugin/test_standalone_cli.py exercises the unreadable-loader branch through an injectable prober seam and asserts both the warning and the applied seccomp marker, and that test fails against the pre-fix wrapper: plugin/test_standalone_cli.py:377-419 test_user_seccomp_unreadable_loader_degrades_and_runs drives the branch via the TERMINAL_JAIL_SECCOMP_READ_PROBE seam (line 397, set to a failing `test -r /nonexistent/...`), asserting rc==0, 'TJDF019-RAN' in stdout, 'Seccomp:\t2' in stdout (line 408), _NEW_READ_WARNING in stderr, the exact loader path in stderr, and the old mapping warning absent. Pre-fix verification: extracted HEAD tree to /tmp/prefix-test, replaced standalone/terminal-jail with the 0fc950a^ version (grep -c SECCOMP_READ_PROBE = 0), ran the same test -> FAILED with 'AssertionError: new warning missing: stderr="terminal-jail: WARNING: no filesystem isolation — could not create a uid mapping..."'. Post-fix: `.venv/bin/python -m pytest plugin/test_standalone_cli.py -p no:cacheprovider -v` -> 21 passed in 4.22s. [resolution 0.10; plugin/test_standalone_cli.py]
  ✓ when the loader IS readable in the candidate launch the wrapper behavior is unchanged: no new warning, mapped launch still chosen: standalone/terminal-jail:545-557: when the readability probe succeeds, _tj_probe_ok=1 -> TERMINAL_JAIL_FS_ISOLATION=mapped and UNSHARE_FLAGS="$_TJ_MAPPED_FLAGS"; no warning is emitted on that branch. Live verification with a fake unshare that succeeds for probes: `PATH=/tmp/fakebin:$PATH TERMINAL_JAIL_SECCOMP_LOADER=$PWD/standalone/seccomp-loader.py ./standalone/terminal-jail --user --seccomp sh -c 'echo ISO=$TERMINAL_JAIL_FS_ISOLATION'` -> no stderr output, stdout 'ISO=mapped'. plugin/test_standalone_cli.py:422-449 test_user_seccomp_read_probe_success_no_degradation asserts neither the new nor the legacy warning appears; it passes (21/21 in the file).
  ✓ the change is confined to standalone/terminal-jail plus its tests: docs/quickstart.md is untouched (owned by TJ-DF-023) and install.sh and the transparent auto-sandbox path in plugin/terminal_jail/interruptor/userns.py are untouched: `git show --name-only --format="" 0fc950a` (the TJ-DF-019 fix commit) lists exactly plugin/test_standalone_cli.py and standalone/terminal-jail. `git diff HEAD -- docs/quickstart.md install.sh plugin/terminal_jail/interruptor/userns.py` is empty (untouched in the working tree). `grep -n 'SECCOMP_READ_PROBE|read_failed|loader not readable'` across those three files returns NONE FOUND. The docs/quickstart.md changes in the commit range come from separate TJ-DF-023 commits (008cb7c, 9652663), and userns.py's mapped_file_access_ok preflight is unchanged (last touched by 9048370, DF-TERMINAL-JAIL-15). [resolution 0.10; docs/quickstart.md, install.sh, plugin/terminal_jail/interruptor/userns.py]


## Summary

Judge Result: TJ-DF-019

Stage tier1: PASS
    ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================

Stage tier2: PASS
  COMPLETE
  ✓ the wrapper probes whether the seccomp loader path is readable inside the candidate uid-mapped launch, and when it is not, falls back to the mapping-less --user launch while printing an explicit degradation warning that names the 0700-home traversal cause and the path it could not read, and the command still runs with seccomp applied: standalone/terminal-jail:537-563: when USE_SECCOMP=1 and SECCOMP_LOADER is set, the wrapper runs `unshare $_TJ_MAPPED_FLAGS test -r "$SECCOMP_LOADER"` (line 546); on failure it sets _tj_read_failed=1 and prints at line 560: "WARNING: seccomp loader not readable under the uid-mapped launch — falling back to a mapping-less --user launch (could not read '${SECCOMP_LOADER}' as the subordinate uid: a home/permission path such as a 0700 or 750 home is not traversable by it; ...)" then sets UNSHARE_FLAGS="--user $UNSHARE_FLAGS" (line 566). Live run: `TERMINAL_JAIL_SECCOMP_READ_PROBE=false TERMINAL_JAIL_SECCOMP_LOADER=$PWD/standalone/seccomp-loader.py ./standalone/terminal-jail --user --seccomp sh -c 'echo TJDF019-RAN; grep Seccomp /proc/self/status'` -> rc=0, stdout 'TJDF019-RAN' + 'Seccomp:	2' + 'Seccomp_filters:	1', stderr contains the warning naming the exact path and the 0700/750-home cause; TERMINAL_JAIL_FS_ISOLATION=degraded. bash -n standalone/terminal-jail = SYNTAX OK.
  ✓ a test in plugin/test_standalone_cli.py exercises the unreadable-loader branch through an injectable prober seam and asserts both the warning and the applied seccomp marker, and that test fails against the pre-fix wrapper: plugin/test_standalone_cli.py:377-419 test_user_seccomp_unreadable_loader_degrades_and_runs drives the branch via the TERMINAL_JAIL_SECCOMP_READ_PROBE seam (line 397, set to a failing `test -r /nonexistent/...`), asserting rc==0, 'TJDF019-RAN' in stdout, 'Seccomp:\t2' in stdout (line 408), _NEW_READ_WARNING in stderr, the exact loader path in stderr, and the old mapping warning absent. Pre-fix verification: extracted HEAD tree to /tmp/prefix-test, replaced standalone/terminal-jail with the 0fc950a^ version (grep -c SECCOMP_READ_PROBE = 0), ran the same test -> FAILED with 'AssertionError: new warning missing: stderr="terminal-jail: WARNING: no filesystem isolation — could not create a uid mapping..."'. Post-fix: `.venv/bin/python -m pytest plugin/test_standalone_cli.py -p no:cacheprovider -v` -> 21 passed in 4.22s. [resolution 0.10; plugin/test_standalone_cli.py]
  ✓ when the loader IS readable in the candidate launch the wrapper behavior is unchanged: no new warning, mapped launch still chosen: standalone/terminal-jail:545-557: when the readability probe succeeds, _tj_probe_ok=1 -> TERMINAL_JAIL_FS_ISOLATION=mapped and UNSHARE_FLAGS="$_TJ_MAPPED_FLAGS"; no warning is emitted on that branch. Live verification with a fake unshare that succeeds for probes: `PATH=/tmp/fakebin:$PATH TERMINAL_JAIL_SECCOMP_LOADER=$PWD/standalone/seccomp-loader.py ./standalone/terminal-jail --user --seccomp sh -c 'echo ISO=$TERMINAL_JAIL_FS_ISOLATION'` -> no stderr output, stdout 'ISO=mapped'. plugin/test_standalone_cli.py:422-449 test_user_seccomp_read_probe_success_no_degradation asserts neither the new nor the legacy warning appears; it passes (21/21 in the file).
  ✓ the change is confined to standalone/terminal-jail plus its tests: docs/quickstart.md is untouched (owned by TJ-DF-023) and install.sh and the transparent auto-sandbox path in plugin/terminal_jail/interruptor/userns.py are untouched: `git show --name-only --format="" 0fc950a` (the TJ-DF-019 fix commit) lists exactly plugin/test_standalone_cli.py and standalone/terminal-jail. `git diff HEAD -- docs/quickstart.md install.sh plugin/terminal_jail/interruptor/userns.py` is empty (untouched in the working tree). `grep -n 'SECCOMP_READ_PROBE|read_failed|loader not readable'` across those three files returns NONE FOUND. The docs/quickstart.md changes in the commit range come from separate TJ-DF-023 commits (008cb7c, 9652663), and userns.py's mapped_file_access_ok preflight is unchanged (last touched by 9048370, DF-TERMINAL-JAIL-15). [resolution 0.10; docs/quickstart.md, install.sh, plugin/terminal_jail/interruptor/userns.py]


Overall: PASS ✓
