# Verdict: TJ-DF-023

**Task:** TJ-DF-023 fix: quickstart §3c must prove the seccomp filter is applied
**Evaluated:** 2026-09-23T14:34:42.937638
**Result:** ✓ PASS

## Pipeline Stages

- ✓ **tier1**
  -   ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================
- ✓ **tier2**
  - COMPLETE
  ✓ docs/quickstart.md section 3c no longer uses echo as the seccomp verification payload and instead names a command whose captured output distinguishes filter-applied from filter-skipped: docs/quickstart.md:222 now reads `terminal-jail --user --seccomp sh -c 'grep Seccomp /proc/self/status'`; captured block at lines 240-244 shows `Seccomp:	2` / `Seccomp_filters:	1`. Ran verbatim on host: output exactly `Seccomp:	2` / `Seccomp_filters:	1` (exit 0). Doc explicitly states 'An `echo`-style payload proves nothing: it runs identically whether or not a filter was installed.'
  ✓ section 3c states the fail-open behaviour of the seccomp loader with a pointer to the loader code path and gives a mount-attempt EPERM check expecting errno 32: docs/quickstart.md:246-252 states 'The filter also **fails open** by design — `standalone/seccomp-loader.py` prints `terminal-jail: seccomp not applied (<reason>); running without seccomp` on stderr and `exec`s the command anyway'. Loader path verified: standalone/seccomp-loader.py:67-68 contains that exact message. Mount EPERM check at lines 258-262: `terminal-jail --user --seccomp sh -c 'mount -t tmpfs none /mnt; echo "mount rc=$?"'` expecting `mount rc=32 — EPERM`; ran verbatim -> `mount: /mnt: must be superuser to use mount.` + `mount rc=32` (exit 0).
  ✓ section 3c documents the 0700-home failure mode of --user --seccomp and cross-references the wrapper degradation warning: docs/quickstart.md:267-274 documents 'On a mapping-capable host whose `$HOME` is `0700` ... `--user --seccomp` cannot read the loader at all and the launch dies with `python3: can't open file '~/.local/lib/terminal-jail/seccomp-loader.py': [Errno 13] Permission denied`; the wrapper prints a loud warning naming that unreadable loader path and keeps the command alive by falling back to the mapping-less launch.' Cross-reference verified against standalone/terminal-jail:560 which emits 'terminal-jail: WARNING: seccomp loader not readable under the uid-mapped launch — falling back to a mapping-less --user launch (could not read '${SECCOMP_LOADER}' ... a home/permission path such as a 0700 or 750 home is not traversable by it ...)'.
  ✓ the change is confined to docs/quickstart.md and every command in the rewritten block that can run on this host was executed verbatim, with non-runnable branches explicitly marked instead of fabricated: `git show 9652663 --name-only` and `git show 008cb7c --name-only` both return only `docs/quickstart.md`. Commands executed verbatim on this host: (1) `terminal-jail --user --seccomp sh -c 'grep Seccomp /proc/self/status'` -> Seccomp:2/Seccomp_filters:1; (2) `terminal-jail --user --seccomp sh -c 'mount -t tmpfs none /mnt; echo "mount rc=$?"'` -> mount rc=32; (3) `python3 scripts/fs-isolation-probe.py` -> DEGRADED (exit 0); (4) `swapon --show` -> prints host swap list. The non-runnable 0700-home branch is framed conditionally ('On a mapping-capable host whose $HOME is 0700...') rather than presented as captured output; this host is DEGRADED (denies uid mapping), and the captured-verbatim block is explicitly labeled 'this project's host, captured verbatim'. Tests: `.venv/bin/python -m pytest plugin/test_standalone_cli.py -q` => '21 passed in 4.11s'; `plugin/test_seccomp.py plugin/test_userns.py -q` => '74 passed, 4 skipped in 3.95s'; LSP diagnostics 0 findings.
All four criteria pass: §3c now proves the seccomp filter via /proc/self/status (verified verbatim), documents fail-open with the loader path and the errno-32 mount probe, documents the 0700-home degradation with the wrapper warning cross-reference, and the change is confined to docs/quickstart.md with runnable commands executed verbatim.

## Summary

Judge Result: TJ-DF-023

Stage tier1: PASS
    ✓ lint: ok (no output)
  ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: ============================= test session starts ==============================

Stage tier2: PASS
  COMPLETE
  ✓ docs/quickstart.md section 3c no longer uses echo as the seccomp verification payload and instead names a command whose captured output distinguishes filter-applied from filter-skipped: docs/quickstart.md:222 now reads `terminal-jail --user --seccomp sh -c 'grep Seccomp /proc/self/status'`; captured block at lines 240-244 shows `Seccomp:	2` / `Seccomp_filters:	1`. Ran verbatim on host: output exactly `Seccomp:	2` / `Seccomp_filters:	1` (exit 0). Doc explicitly states 'An `echo`-style payload proves nothing: it runs identically whether or not a filter was installed.'
  ✓ section 3c states the fail-open behaviour of the seccomp loader with a pointer to the loader code path and gives a mount-attempt EPERM check expecting errno 32: docs/quickstart.md:246-252 states 'The filter also **fails open** by design — `standalone/seccomp-loader.py` prints `terminal-jail: seccomp not applied (<reason>); running without seccomp` on stderr and `exec`s the command anyway'. Loader path verified: standalone/seccomp-loader.py:67-68 contains that exact message. Mount EPERM check at lines 258-262: `terminal-jail --user --seccomp sh -c 'mount -t tmpfs none /mnt; echo "mount rc=$?"'` expecting `mount rc=32 — EPERM`; ran verbatim -> `mount: /mnt: must be superuser to use mount.` + `mount rc=32` (exit 0).
  ✓ section 3c documents the 0700-home failure mode of --user --seccomp and cross-references the wrapper degradation warning: docs/quickstart.md:267-274 documents 'On a mapping-capable host whose `$HOME` is `0700` ... `--user --seccomp` cannot read the loader at all and the launch dies with `python3: can't open file '~/.local/lib/terminal-jail/seccomp-loader.py': [Errno 13] Permission denied`; the wrapper prints a loud warning naming that unreadable loader path and keeps the command alive by falling back to the mapping-less launch.' Cross-reference verified against standalone/terminal-jail:560 which emits 'terminal-jail: WARNING: seccomp loader not readable under the uid-mapped launch — falling back to a mapping-less --user launch (could not read '${SECCOMP_LOADER}' ... a home/permission path such as a 0700 or 750 home is not traversable by it ...)'.
  ✓ the change is confined to docs/quickstart.md and every command in the rewritten block that can run on this host was executed verbatim, with non-runnable branches explicitly marked instead of fabricated: `git show 9652663 --name-only` and `git show 008cb7c --name-only` both return only `docs/quickstart.md`. Commands executed verbatim on this host: (1) `terminal-jail --user --seccomp sh -c 'grep Seccomp /proc/self/status'` -> Seccomp:2/Seccomp_filters:1; (2) `terminal-jail --user --seccomp sh -c 'mount -t tmpfs none /mnt; echo "mount rc=$?"'` -> mount rc=32; (3) `python3 scripts/fs-isolation-probe.py` -> DEGRADED (exit 0); (4) `swapon --show` -> prints host swap list. The non-runnable 0700-home branch is framed conditionally ('On a mapping-capable host whose $HOME is 0700...') rather than presented as captured output; this host is DEGRADED (denies uid mapping), and the captured-verbatim block is explicitly labeled 'this project's host, captured verbatim'. Tests: `.venv/bin/python -m pytest plugin/test_standalone_cli.py -q` => '21 passed in 4.11s'; `plugin/test_seccomp.py plugin/test_userns.py -q` => '74 passed, 4 skipped in 3.95s'; LSP diagnostics 0 findings.
All four criteria pass: §3c now proves the seccomp filter via /proc/self/status (verified verbatim), documents fail-open with the loader path and the errno-32 mount probe, documents the 0700-home degradation with the wrapper warning cross-reference, and the change is confined to docs/quickstart.md with runnable commands executed verbatim.

Overall: PASS ✓
